import { describe, expect, it } from 'vitest';

import { VERIFICATION_TIER2_SUBTITLE, VERIFICATION_BENEFIT_BULLETS, VERIFICATION_SIDEBAR_SUBLABEL, VERIFICATION_PREVIEW_CAPTION, VERIFICATION_PREVIEW_FALLBACK_NAME, VERIFICATION_REVIEW_TIMELINE, VERIFICATION_TIER2_APPROVED, VERIFICATION_TIER2_DOC_LABELS, VERIFICATION_TIER2_DOC_HELP } from '@/features/dashboard/org/lib/verificationCopy';

describe('VERIFICATION_TIER2_SUBTITLE', () => {
  it('is defined', () => {
    expect(VERIFICATION_TIER2_SUBTITLE).toBeDefined();
  });
});

describe('VERIFICATION_BENEFIT_BULLETS', () => {
  it('is defined', () => {
    expect(VERIFICATION_BENEFIT_BULLETS).toBeDefined();
  });
});

describe('VERIFICATION_SIDEBAR_SUBLABEL', () => {
  it('is defined', () => {
    expect(VERIFICATION_SIDEBAR_SUBLABEL).toBeDefined();
  });
});

describe('VERIFICATION_PREVIEW_CAPTION', () => {
  it('is defined', () => {
    expect(VERIFICATION_PREVIEW_CAPTION).toBeDefined();
  });
});

describe('VERIFICATION_PREVIEW_FALLBACK_NAME', () => {
  it('is defined', () => {
    expect(VERIFICATION_PREVIEW_FALLBACK_NAME).toBeDefined();
  });
});

describe('VERIFICATION_REVIEW_TIMELINE', () => {
  it('is defined', () => {
    expect(VERIFICATION_REVIEW_TIMELINE).toBeDefined();
  });
});

describe('VERIFICATION_TIER2_APPROVED', () => {
  it('is defined', () => {
    expect(VERIFICATION_TIER2_APPROVED).toBeDefined();
  });
});

describe('VERIFICATION_TIER2_DOC_LABELS', () => {
  it('is defined', () => {
    expect(VERIFICATION_TIER2_DOC_LABELS).toBeDefined();
  });
});

describe('VERIFICATION_TIER2_DOC_HELP', () => {
  it('is defined', () => {
    expect(VERIFICATION_TIER2_DOC_HELP).toBeDefined();
  });
});
