import { describe, it } from 'vitest';

/** Types-only module — runtime values covered via consumers. */
describe.skip('telegramEnvVerify', () => {
  it('N/A in Vitest Node (types-only)', () => {});
});
