import { describe, it } from 'vitest';

/** Polotno + react-color pull in browser-only modules — covered by marketing E2E / manual QA. */
describe.skip('initPolotno (browser-only)', () => {
  it('N/A in Vitest Node', () => {});
});
