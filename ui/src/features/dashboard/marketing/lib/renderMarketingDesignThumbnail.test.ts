import { describe, it } from 'vitest';

/** Depends on Polotno canvas runtime — covered by marketing collage E2E. */
describe.skip('renderMarketingDesignThumbnail (browser-only)', () => {
  it('N/A in Vitest Node', () => {});
});
