/**
 * Mirrors server caps: `_shared/guestChatAttachments.ts#MAX_WEB_MESSAGE_CHARS` (guest + inbox web
 * chat) and `dashboard-assistant-chat` MAX_MESSAGE_CHARS (AI assistant).
 */
export const CHAT_MESSAGE_MAX_CHARS = 4_000;
