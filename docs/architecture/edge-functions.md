---
title: 'Edge functions (API surface)'
status: active
tags: [architecture, edge-functions]
updated: 2026-09-17
---

# Edge functions (API surface)

Part of the [`docs/PROJECT.md`](../PROJECT.md) architecture split.

---

## Pagination contract

Standard shape for any **new** list-returning edge function (Phase 10.3, [`10-paginate-large-lists.md`](../workflow/planned/production-readiness-checklist/10-paginate-large-lists.md)). Existing endpoints are **not** migrated to this wholesale — that is an explicitly out-of-scope, separate, bigger change. Apply it to new pagination work as it's written; reference implementations: `list-activity-log`, `notifications-list`, and the `listBookings` SQL fast path in `_shared/databaseService.ts`.

**Request** (query params on GET, or body fields on POST):

```ts
{
  limit?: number;    // default 25, max 100 — SERVER clamps, never trusts the client value
  cursor?: string;   // opaque keyset cursor from a prior response's nextCursor; omit for page 1
  sort?: string;      // e.g. "created_at:desc" — must resolve to a column with a unique tiebreaker
  filters?: Record<string, unknown>; // endpoint-specific; changing any filter must reset cursor
}
```

**Response:**

```ts
{
  items: T[];
  nextCursor: string | null; // null once the last page is reached
  totalCount?: number;       // optional, expensive — see rule below
}
```

Rules:

- **Server clamps the limit.** `Math.min(MAX_LIMIT, Math.max(1, requested || DEFAULT_LIMIT))` — a client asking for `limit=100000` gets `MAX_LIMIT`. This is a denial-of-service control, not a convenience (doc 23).
- **Keyset (cursor) over offset** for anything large or realtime-fed (bookings, messages, notifications, activity). Offset pagination re-scans and skips/duplicates rows when items are inserted between page fetches, which this app does constantly.
- **Over-fetch-by-one trick** to detect `hasMore` without a second query: request `limit + 1` rows, and if more than `limit` come back, slice to `limit` and set `nextCursor` from the last row of the sliced page. Both reference implementations use this — see `list-activity-log`'s `.limit(limit + 1)` and the `hasMore`/`page` split in `notifications-list`.
- **Deterministic ordering.** Every sort must include a unique tiebreaker (typically `id`) alongside the primary sort column, or the cursor is unstable and rows repeat or vanish across pages. `list-activity-log` orders `created_at DESC, id DESC` and encodes both into the cursor.
- **Cursor encodes `(sort_key, id)`**, not just an offset or a single timestamp — a timestamp alone collides when two rows share it. Example keyset predicate: `created_at.lt.<ts>,and(created_at.eq.<ts>,id.lt.<id>)`.
- **`totalCount` is optional and expensive.** A `COUNT(*)` over a large filtered set is its own performance problem — prefer "load more" / infinite scroll over numbered pages. Where a count is genuinely required (an admin table with page numbers), use `count: 'exact'` sparingly and consider caching it (doc 12) rather than recomputing on every page fetch.
- **Filter change resets the cursor.** The client must not reuse a cursor across a filter/sort change — include the filter/sort in the query key so React Query treats it as a fresh query.
- **Scope is not optional.** Every paginated query must apply its org/property/parking scope in the _same_ query as the pagination — a bound without a scope means page 2 can leak another tenant's rows (cross-check doc 22, the `assertBookingBelongsToProperty` class of bug from the launch audit).
- **`.range()` is inclusive in PostgREST** — `range(0, 24)` is 25 rows, not 24. Prefer `.limit(n)` over hand-computed `.range()` offsets where the shape above (limit + cursor) already avoids offset math entirely.

Full phase status, the Phase 10.1 audit table, and what's fixed vs. deferred: [`10-paginate-large-lists.md`](../workflow/planned/production-readiness-checklist/10-paginate-large-lists.md).

---

## Payload shape contract

JSON compresses well, so shrinking response _shape_ (doc 13, [`13-compress-api-payloads.md`](../workflow/planned/production-readiness-checklist/13-compress-api-payloads.md)) matters more than transport compression. Rules for any **new** query or endpoint:

- **Never `select('*')` on a wide or tenant-growing table.** Select explicit columns. A table qualifies as "wide" once it accumulates JSONB blobs, many text columns, or per-document AI verdict/summary pairs (`guest_submissions` is the reference example — 80+ columns across 10+ migrations). A small, genuinely narrow config/catalog table (org/property/parking rows, team-member rows, plan catalog, settings tables — row counts in the tens, not thousands) is a lower-priority target; `select('*')` there is tolerated, not encouraged.
- **A JSONB settings blob belongs on the detail endpoint, not the list endpoint**, unless the list UI actually renders a field from it — check the consumer before deciding either way.
- **CI enforces this at the boundary, not by line-by-line review**: `scripts/dev/check-select-star.sh` flags any new `select('*')`/`select("*")` in `supabase/functions/**` outside its allowlist — same shape as `check-unbounded-select.sh` and `check-cache-class.sh`. The allowlist is a snapshot of every call site as of doc 13, not a claim that each one is optimal; narrowing one should remove it from the allowlist so the guard protects the win.
- **Request payloads use a patch shape, not a full-object overwrite**, for anything with a strict column allowlist and RBAC-per-field — `_shared/bookingDetailsPatch.ts`'s `BOOKING_PATCH_ALLOWED_COLUMNS` + `sanitizeBookingPatchPayload` is the reference pattern: an explicit allowlist, per-column validation, per-column permission mapping.
- **Never base64-encode media in a client-facing JSON body.** Use Storage direct-upload (`_shared/uploadService.ts`'s `File`/multipart pattern) or a signed URL. The one sanctioned exception in this codebase is `_shared/dashboardAssistantAttachments.ts` (AI assistant chat attachments) — small, hard-capped (4 MiB × 3 files), and immediately persisted to Storage rather than stored as base64 anywhere; do not use it as precedent for a general upload path.
- **BREACH/CRIME awareness**: never place user-reflected free-text alongside a capability/session/CSRF token in the same response body if that response could ever be served compressed to an attacker who can vary the reflected content. `get-form`'s signed Storage URLs alongside guest-editable fields is a documented, reviewed, low-severity instance of this shape (doc 13's Implementation status section) — read that before adding a similar pattern elsewhere.
- **Exports scope before they stream.** `_shared/financeExport.ts`'s CSV builder is not a true stream (single in-memory string), but is scoped by property + status + SQL-pushed period range (doc 10) rather than fetching a whole tenant's history — that scoping is the load-bearing bound, not the missing streaming. A genuinely unbounded export (whole-org, whole-history) needs either true chunked streaming or a hard row cap (`activity-log-export/index.ts`'s `MAX_ROWS = 20_000` is the reference cap pattern), not just "scope it and call it done."

Full phase status and the Phase 13.2 classification of all 102 `select('*')` call sites: [`13-compress-api-payloads.md`](../workflow/planned/production-readiness-checklist/13-compress-api-payloads.md).

---

## Response caching contract

Every JSON response declares an explicit `Cache-Control` (Phase 11.1, [`11-cache-api-responses.md`](../workflow/planned/production-readiness-checklist/11-cache-api-responses.md)). `_shared/httpResponse.ts`'s `jsonResponse(req, body, status, cacheClass)` / `jsonSuccess(req, data, extra, cacheClass)` take a trailing `CacheClass` argument; **omit it and the response gets `private, no-store`** — the safe, fail-closed default for every admin/tenant/guest-PII-scoped read. Existing call sites are unaffected: the parameter was added as optional/trailing specifically so the ~448 pre-existing call sites across the tree stay source-compatible.

```ts
import { jsonSuccess, jsonSuccessWithETag } from '../_shared/httpResponse.ts';

// Default — no class needed for admin/tenant/guest reads and all mutations:
return jsonSuccess(req, data);

// Genuinely public, non-personalized data — pass the class explicitly:
return jsonSuccess(req, data, undefined, 'publicDynamic');

// Public + stable payload (no timestamp, no signed URL) — ETag + 304 support:
return jsonSuccessWithETag(req, data, 'publicStatic');
```

| Class                | `Cache-Control`                                                    | Use for                                                                                         | Example call sites                                                                                                                                                                                                                                          |
| -------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `publicStatic`       | `public, max-age=300, s-maxage=3600, stale-while-revalidate=86400` | Plan catalog, vocabularies — changes only on a super-admin edit                                 | `list-public-pricing-plans`, `get-residence-unit-types`                                                                                                                                                                                                     |
| `publicDynamic`      | `public, max-age=60, s-maxage=300, stale-while-revalidate=600`     | Property/parking listings, detail pages, search results, public host profiles                   | `get-public-property`, `get-public-parking`, `get-public-host`, `get-public-showcase` (published branch), `list-public-properties`, `list-public-parkings`, `list-public-developments`, `list-public-place-groups`, `search-listings`, `search-suggestions` |
| `publicAvailability` | `public, max-age=30, s-maxage=60`                                  | Calendar availability, pricing — short TTL and **no** SWR; stale availability = double bookings | `get-booked-dates`                                                                                                                                                                                                                                          |
| `private` (default)  | `private, no-store`                                                | Every admin/host/org/property/parking read; anything with no explicit class                     | Implicit for ~250 remaining functions — see doc 11's Implementation status for what was and wasn't hand-audited                                                                                                                                             |
| `guestToken`         | `no-store`                                                         | Guest PII / capability-token reads (`get-form`, stay guide, signed-URL issuers)                 | Not separately annotated — already `no-store` via the `private` default; see doc 11                                                                                                                                                                         |
| `mutation`           | `no-store`                                                         | All POST/PATCH/DELETE                                                                           | Implicit via the default                                                                                                                                                                                                                                    |

Rules:

- **Fail closed.** A call site that forgets to pass a class is `private, no-store`, never guessable-cacheable. `scripts/dev/check-cache-class.sh` (CI-wired) is a two-way guard: every function in its `PUBLIC_CACHE_FILES` allowlist must use a `public*` class somewhere, and no function **outside** that allowlist may use one — catches both "a public endpoint silently regressed to private" and "a `public*` class leaked onto a tenant-scoped response" (a cross-tenant data leak).
- **`Vary: Origin, Accept-Encoding`** is added automatically to every `public*`-classed response (`_shared/cors.ts` already varies by `Origin` for CORS; `Accept-Encoding` covers gzip/br variants). Never added to `private`/`no-store` responses — nothing stores them, so `Vary` there is meaningless.
- **ETag (`jsonSuccessWithETag`)** is only for a payload that's stable across requests for the same query. **Never** call it for a payload embedding a timestamp or a freshly signed Storage URL — the ETag changes every request and produces zero `304`s for pure overhead. Use plain `jsonSuccess(..., 'publicDynamic')` for those and skip ETag.
- **Maintenance mode is the canonical "don't cache this even though it's public" example** — `get-public-platform-status` stays on the `private` default deliberately, even though it's an anon-safe read, because `platform_settings.maintenanceMode` must never be served from a longer-lived cache than the existing 60s in-memory `platformSettingsCache` path.
- **A `public*` directive on an authenticated/tenant-scoped response is a cross-tenant data leak** — treat every new `public*` call site as a security-reviewed decision (verify: no auth check gates the handler, no per-viewer variation in the payload), not a performance tweak.

Full phase status — what's classified vs. deferred, real bugs found during self-review (the CI guard's `rg`-not-on-`PATH` fallback bug), and the SW allowlist reconciliation: [`11-cache-api-responses.md`](../workflow/planned/production-readiness-checklist/11-cache-api-responses.md).

---

## Logging & correlation IDs contract (doc 27)

`_shared/requestLog.ts#logEvent` emits one JSON shape: `{ts, level, fn, requestId, orgId?, propertyId?, userId?, event, durationMs?, status?, meta?}`. `level` is `error` (needs a human) / `warn` (degraded, self-healing) / `info` (state change) / `debug` (off unless `EDGE_LOG_DEBUG=1`).

- **Wired centrally, not per-function.** `handleEdgeError` (`_shared/httpResponse.ts`) — the one place every `serve*` wrapper in `serveEdge.ts` already routes a caught error through — calls `logEvent` on every failure. This gives all ~300 functions the structured shape without a per-function edit; a handler only calls `logEvent` directly for a mid-request state change worth recording beyond the wrapper's own entry/error line.
- **Correlation id**: `resolveRequestId(req)` reads a client-sent `x-request-id` header (validated `^[a-zA-Z0-9_-]{1,64}$`, otherwise `crypto.randomUUID()` server-side). Threaded into the structured log, the PostHog exception's `extra.requestId`, and — **only on a 5xx** — the JSON error body's `requestId` field (a 4xx is usually expected client flow; a reference id there is noise). `jsonError(req, message, status, requestId?)` — the 4th param is optional and backward compatible.
- **Client side (admin surface only):** `ui/src/lib/api/adminEdgeFetch.ts` sends a fresh `x-request-id` on every `adminEdgeFetch`/`assetEdgeFetch` call; `AdminEdgeFetchError.requestId` carries it back on failure. Guest-side fetch call sites do not send this header yet — extending the pattern there is the natural next slice, not done this session.
- **Never log:** guest names, emails, phone numbers, addresses, ID/document contents, tokens, secrets, full request bodies. Log identifiers (`bookingId`), not the row/object. A PII-in-logs audit of all `console.*` call sites across `supabase/functions/**` found and fixed 3 violations of this rule (2026-09-19) — see doc 27's implementation status for specifics.

---

## Query result cache (`query_cache` table)

Doc 11 above caches the **HTTP response**. `_shared/queryCache.ts` caches the **computation**
behind it, for a query that's expensive and shared across viewers but not safely publishable via
`Cache-Control` (doc 12, [`12-cache-expensive-queries.md`](../workflow/planned/production-readiness-checklist/12-cache-expensive-queries.md)). Shape mirrors `_shared/aiQuotaCache.ts`: a
deterministic key, an explicit TTL column, best-effort reads/writes that never fail the caller.

**Table** `query_cache` (migration `20261316121700_query_cache_table.sql`):

| Column              | Type          | Notes                                                                            |
| ------------------- | ------------- | -------------------------------------------------------------------------------- |
| `cache_key`         | `text` PK     | Built by `buildCacheKey()` — schema-versioned, scope-complete, hashed            |
| `payload`           | `jsonb`       | The cached computation result (or a short-lived `computing` sentinel, see below) |
| `scope_org_id`      | `uuid null`   | Denormalized for wholesale tenant purge without decoding the key                 |
| `scope_property_id` | `uuid null`   | Same                                                                             |
| `scope_parking_id`  | `uuid null`   | Same                                                                             |
| `computed_at`       | `timestamptz` | Write time                                                                       |
| `expires_at`        | `timestamptz` | TTL, jittered ±10% per entry at write time                                       |
| `schema_version`    | `int`         | Bumped in `queryCache.ts` on any payload-shape change — invalidates every key    |

No RLS policy — `service_role` only, same as other service-role-only tables (see this repo's
"RLS is not the access-control layer today" note). Never queried from a client-side Supabase call.

**Security rule — read before adding a call site:** `buildCacheKey()` takes an explicit
`permissionScope` argument. A cache key that omits a viewer-permission dimension (e.g. a scoped
org admin's assigned-listing set vs. an all-listings admin) can serve one viewer's cached response
to a differently-privileged viewer for the same org/property/parking id — the doc's own "most
dangerous bug in this doc." Pass `permissionScope: null` only when the endpoint's payload cannot
vary by viewer for a given scope id (e.g. a single-property request already access-checked
upstream of the cache read).

**Wired call site:** `dashboard-stats` (`GET /functions/v1/dashboard-stats`) — the admin home
dashboard aggregate, polled every 60s by every open property/org/parking dashboard tab
(`ui/src/features/dashboard/{property,org,parking}/hooks/use*DashboardStats.ts`) and backed by
`_shared/dashboardService.ts#computeDashboardStats`, which does an unbounded `select('*')` on
`guest_submissions` in every scope branch. Cached 45s TTL (±10% jitter); org-scope requests key on
the resolved `scopedPropertyIds`/`scopedParkingIds` set via `permissionScope` so a scoped admin
and an all-listings admin on the same org never share a cache entry.

**Invalidation:** routed through `_shared/workflowOrchestrator.ts` (`purgeCacheByScope`, called
right after the activity-log/PostHog side effects on every `saveToDatabase` transition) — never
scattered into individual handlers, per this repo's side-effects-never-inline rule. A transition
purges both the property-scoped and the resolved org-scoped cache entries.

**Stampede protection (doc 12, Phase 12.4) — v1, honestly scoped:** a short-lived `computing`
sentinel row acts as a soft lock; a request landing while another is computing the same key falls
through and computes independently rather than durably waiting (no cross-instance wait/notify
primitive is used). This is "first request wins, close-together duplicates both compute," not true
single-flight — documented in `queryCache.ts`'s file header.

**Sweep cron:** `query-cache-sweep-cron`, nightly, deletes expired rows —
[`scheduled-jobs-and-testing.md`](../archive/operations/scheduled-jobs-and-testing.md).

**Not yet wired:** every other candidate the doc's Phase 12.1 flags by inspection
(`analyticsService.ts`, `financeService.ts`, `propertyListStats.ts`, `publicSearch.ts`,
`publicListingFacets.ts`, `superhostMetrics.ts`, `smartPricingEngine.ts`,
`inboxThreadMetrics.ts`) — see doc 12's Implementation status section for the reasoned-but-
unmeasured prioritization and what's deferred.

---

## Conformance sweep (doc 18 — Backend & APIs)

Doc 18's Phase 18.1 asks for a conformance table across all ~300 functions: wrapper, auth
tier, validation, response shape, idempotency, bounded queries, activity log, tests, callers.
That table is **generated, not hand-maintained** — regenerate it instead of trusting a stale
copy pasted here:

```bash
node scripts/dev/audit-edge-functions.mjs             # markdown table + summary to stdout
node scripts/dev/audit-edge-functions.mjs --csv       # CSV instead of markdown
node scripts/dev/audit-edge-functions.mjs --gaps-only # only rows with >=1 flagged gap
node scripts/dev/audit-edge-functions.mjs --check     # CI: fail on new hand-rolled serve()
# also: bun run audit:edge-functions / bun run check:edge-conformance
```

The script walks every `supabase/functions/<fn>/index.ts` (skipping `_shared/` and `tests/`)
and statically flags, via regex/string search: which `serve*` wrapper is used (or hand-rolled
`serve(`/`Deno.serve(` with no wrapper); whether a validation helper is detected (`zod`,
`.parse(`, `.safeParse(`); whether `jsonSuccess`/`jsonError` is used; whether
`_shared/idempotency.ts` is imported; whether a `.select(` call has a matching `.limit(`/`.range(`
anywhere in the file; and whether `_shared/activityLog.ts` is referenced or an
`activity-log: N/A` comment is present. **This is a heuristic triage list, not ground truth** —
every flagged row needs a human look before being treated as a real defect (see findings below).

### Sweep snapshot (2026-09-17)

296 functions audited (of 297 directories; `node_modules` under `supabase/functions/` is a
package-manager artifact, not a function, and is skipped automatically since it has no
`index.ts`).

| Wrapper               | Count |
| --------------------- | ----- |
| `serveAuthenticated`  | 197   |
| `serveSuperAdmin`     | 32    |
| `servePublic`         | 30    |
| Hand-rolled `serve()` | 19    |
| `serveCronPost`       | 17    |
| `serveAdmin`          | 1     |

Heuristic gap counts (each needs individual review — see caveats below before treating any of
these as a confirmed defect):

- 19 functions with no `serve*` wrapper (hand-rolled `serve()`/`Deno.serve()`).
- 290 functions with no validation helper detected by the heuristic (see caveat — most
  validate by hand without `zod`/`.parse(`, which this sweep can't see).
- 36 functions with no `jsonSuccess`/`jsonError` call detected.
- 293 functions with no `_shared/idempotency.ts` import (expected — idempotency is only wired
  for `transition-booking`, `transition-parking-booking`, and `social-inbox-send` today, see
  the API surface table below).
- 87 functions with a `.select(` call but no `.limit(`/`.range(` anywhere in the file (doc 10
  cross-check — a lead for the paginate-large-lists follow-up, not a confirmed unbounded query;
  many of these are single-row lookups by primary key that don't need bounding).
- 261 functions with no `_shared/activityLog.ts` reference and no `activity-log: N/A` comment
  (expected — most reads never need to log; this flags every GET alongside every write, so it
  over-counts heavily. Doc 18's real ask is that every **mutating** function either logs or
  justifies N/A, which this heuristic cannot distinguish without knowing which functions write).

**Investigated during this pass — all 19 hand-rolled `serve()` hits turned out to be
legitimate, not bypasses:**

| Function                                                                                                                                                                                                 | Why it's hand-rolled                                                                                                                                                                                                                                                             |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ical-export`                                                                                                                                                                                            | Returns `text/calendar`, not JSON — the `serve*` wrappers assume a JSON response shape.                                                                                                                                                                                          |
| `meta-inbox-oauth-callback`                                                                                                                                                                              | Returns HTTP redirects (Meta OAuth dance), not JSON.                                                                                                                                                                                                                             |
| `parking-broadcast-email`                                                                                                                                                                                | Retired stub — always returns 410, no auth surface to wrap.                                                                                                                                                                                                                      |
| `submit-form`, `submit-form-completion`, `submit-pay-parking`, `submit-sd-form`                                                                                                                          | Public anon writes gated by `antiSpamGate` (heuristics + CAPTCHA + rate limit) inline — a hand-rolled shape that predates `servePublic`, but auth-equivalent, not a bypass.                                                                                                      |
| `calendar-sync-cron`, `sd-refund-cron`                                                                                                                                                                   | Dual-mode: cron secret gate **or** admin JWT + `resolveScopedPropertyAccess` for the "Sync now" / scoped path — doesn't fit the single-tier `serveCronPost`/`serveAdmin` shape as written.                                                                                       |
| `superhost-assessment-cron`                                                                                                                                                                              | Dual-mode: cron secret **or** `verifySuperAdminJwt`.                                                                                                                                                                                                                             |
| `meta-inbox-backfill`                                                                                                                                                                                    | `verifyAuthenticatedUser` called manually inline — functionally equivalent to `serveAuthenticated`, just not migrated.                                                                                                                                                           |
| `meta-inbox-webhook`                                                                                                                                                                                     | Provider webhook (Meta) — signature/rate-limit verification inline, provider-mandated response shape.                                                                                                                                                                            |
| `upload-app-settings-asset`, `upload-org-settings-asset`, `upload-development-media`, `upload-parking-media`, `upload-parking-settings-asset`, `upload-property-media`, `upload-property-template-asset` | Multipart file uploads — each calls the correct guard manually (`verifyAdminJwt`, `verifySuperAdminJwt`, `resolveScopedPropertyAccess`, or `resolveScopedParkingAccess`) but predates the wrapper convention, likely because multipart bodies don't flow through `readJsonBody`. |

**None of these were converted in this pass.** Migrating a hand-rolled handler to a `serve*`
wrapper can change CORS/error-envelope/method-handling behavior in subtle ways (the wrappers
assume a single auth tier and a JSON body) — that needs per-function review and a handler test
pass, not a batch edit. Each row above already enforces its own equivalent auth check; the gap
is convention/consistency, not a security bypass. Flagged for a future individually-reviewed
pass; not re-litigated by re-running the script.

**`jsonSuccess`/`jsonError` "gap" is mostly a false positive on inspection**: the 36 flagged
functions split into (a) `serveCronPost` handlers, which get their own success/error envelope
from the wrapper itself (`_shared/serveEdge.ts`'s `serveCronPost` calls `jsonResponse`/`jsonError`
internally — a handler using it correctly never calls `jsonSuccess` directly), (b) the three
non-JSON hand-rolled functions above, and (c) a handful (`list-bookings`, `transition-booking`,
the `upload-*` functions) that call the lower-level `jsonResponse` directly or hand-build the
`{ success, data }` shape rather than the `jsonSuccess`/`jsonError` convenience wrappers —
same envelope shape, different call site. Standardizing those onto `jsonSuccess`/`jsonError`
is a real, low-risk follow-up but was not done in this pass to keep the change mechanical-only.

**Dead/unused functions (doc 05 cross-check):** doc 05
([`05-unused-dependencies.md`](../workflow/planned/production-readiness-checklist/05-unused-dependencies.md))
already looked at this and explicitly deferred it — `knip` doesn't analyze Deno code at all, and
a trustworthy dead-edge-function list needs a manual pass cross-referencing `supabase/config.toml`,
`pg_cron` schedules, and provider webhook configs, not a static sweep. Not re-attempted here;
still open as Phase 5.5 in doc 05 and Phase 18.1's "Callers" column in doc 18.

**Not yet wired into CI** (doc 18 Phase 18.7) — the script is a manual/on-demand tool today,
same status as `check-serve-public-rate-limit.sh` before it was CI-wired. Wiring it in with a
committed baseline (fail on regressions, not on the current gap count) is deferred to a future
pass so the large existing gap count doesn't immediately red-flag CI.

Full phase status, phase-by-phase plan, and exit gate: [`18-backend-and-apis.md`](../workflow/planned/production-readiness-checklist/18-backend-and-apis.md).

---

## 8. Edge functions (API surface)

| Function                             | Method       | Auth                                            | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------ | ------------ | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `submit-form`                        | POST         | anon (public)                                   | Multipart form processing, overlap check, change detection, DB + storage (optional calendar/sheet via FormData flags in non-prod; production forces all on), **no PDF**; **no workflow emails**; optional **`sendEmail`** FormData (default on unless `sendEmail=false`) gates **New Booking Request** notify **to `EMAIL_REPLY_TO` only** (`sendNewBookingRequestNotify`). Optional FormData **`appliedVoucherSourceBookingId`** + guest JWT redeems a same-property next-stay voucher onto the new booking. **AI receipt/ID validation is intentionally not performed on the public submission**; admins run that from the booking detail page. **Telegram** new-booking marketing line (non-fatal) when DB row is first inserted and `telegram_marketing_settings` + bot env allow — see **[[telegram-marketing-reminders]]**. Updates from `/form` while status is revert-eligible (pending-docs stages or `READY_FOR_CHECKIN`) set `PENDING_REVIEW` only when `compareFormData` changed fields are workflow-sensitive (see §5.2) |
| `get-form`                           | GET          | anon (public)                                   | Path: `/get-form/{bookingId}` — returns JSON form payload + **`guestCanUpdate`** (only **`true`** when `status === PENDING_REVIEW`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `get-form-completion`                | GET          | anon (public, token)                            | **Calendar sync Phase 2** (`?complete=<token>`). Resolves the guest-form completion token → `{ propertySlug, locked: true, stay: { checkInDate, checkOutDate, checkInTime, checkOutTime, numberOfNights }, prefill: { primaryGuestName, bookingSource } }`. **404** unknown/rotated token; **410** once the stay has ended or the booking is `CANCELLED`. **`verify_jwt = false`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `submit-form-completion`             | POST         | anon (public, token)                            | **Calendar sync Phase 2.** Multipart form-data (same shape as `submit-form`) + `complete=<token>`. **UPDATE-only** — never inserts; `check_in/out` date+time+`number_of_nights`+`booking_source`+`external_*` are re-read from the stored row (payload dates ignored); guarded on `status = 'PENDING_REVIEW'` (409 otherwise); overlap scan excludes the row + skips its own `ical_import` block; stamps `guest_form_completed_at`, emits **`booking_guest_form_completed`**. `verify_jwt = false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `get-guest-payment-info`             | GET          | anon (public)                                   | Returns payment/branding/GAF defaults, guest form toggles (`allowPets`, `allowParking`, `allowSurpriseDecor`), **check-in/out times** (`checkInTime`, `checkOutTime`; defaults **`14:00`** / **`12:00`**), **guest capacity** (`maxAdults`, `maxChildren`; defaults **`4`** / **`1`**) from **`properties.settings`**, and **property branding** (`propertyName`, `propertyEyebrow`, `propertyCoverImageUrl`, `residenceName`, `organizationName`) via **`_shared/guestFormSettings.ts`**. Guest form uses times + capacity on Stay step; **`submit-form`** clamps pet/parking/decor and asserts party occupancy rules.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `get-residence-unit-types`           | GET          | anon (public)                                   | **`?residenceName=`** — **`unitTypes`** from **`developments.settings.unitTypes`** (`id`, `label`, `bedrooms`, `bathrooms`, `maxAdults`, `maxChildren`); Azure North defaults: Studio 1/1 bed/bath 4/1 guests, 1BR 1/1 6/2, 2BR 2/1 8/3. Property settings unit-type dropdown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `get-public-property`                | GET          | anon (public)                                   | **`?property=<slug>`** or **`?property_id=`**. Returns **ACTIVE** property marketing profile: media, amenities, location, check-in/out, host (owner + org + slug), pricing defaults, house rules (`enabledHouseRules` / `customHouseRules` in settings). **404** when missing or **INACTIVE**. Guide: **[[public-property-catalog]]**, **[[guides/routes/properties]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `get-public-showcase`                | GET          | anon (public)                                   | **`?property=<slug>`**. Composes property detail + `custom_pages` showcase `template_key` + `public_page_configs` `PropertyShowcaseConfig` + **`guestContact`**. Without **`propertyShowcase`** (and not `?embed=1`/`preview=1`) → `{ planAccessDenied: true }` (no property leak). Unpublished → lightweight “not available” unless embed/preview. Legacy showcase section id **`houseRules`** normalized to **`host`** on read. **`verify_jwt = false`**. Guide: **[[guides/routes/property-showcase]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `get-public-host`                    | GET          | anon (public)                                   | **`?org=<org_slug>`**. Public org/host profile for **`/hosts/:orgSlug`**: **`brandColor`**, org name/logo, tagline, description, owner name + avatar, **`verifiedBadge`** (enhanced verification approved), **`isSuperhost`** (earned org Superhost), **`socialLinks`**, **ACTIVE** **`properties`** + **`parkings`**. Listing card rates via batched **`app_settings`** / **`parking_settings`** reads (no per-listing settings seed). **404** when org slug missing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `get-booked-dates`                   | GET          | anon (public)                                   | Future, non-`CANCELLED` booking ranges                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ical-export`                        | GET          | anon (public, token)                            | **Outbound calendar feed** (`calendarSync`, Pro+). `?property=<slug>&token=<t>[&as=<provider>]` → RFC-5545 `text/calendar` of GFM busy nights (non-`CANCELLED`/`IMPORTED` bookings + `source='manual'` blocks). `?as=airbnb\|booking_com\|vrbo` omits that provider's own `ical_import` rows (echo-loop guard). Constant-time token compare; **every** failure (bad slug, wrong/rotated token, export disabled) returns an identical **404**. Weak `ETag` + `If-None-Match` **304**, `Cache-Control: public, max-age=900`, `Last-Modified`. Token from **`property_calendar_export`**; `last_served_at` updated best-effort. **`verify_jwt = false`**.                                                                                                                                                                                                                                                                                                                                                                                |
| `cancel-booking`                     | POST         | admin JWT                                       | Soft-cancel via `WorkflowOrchestrator`; updates Calendar (purple, `CANCELED - …`) + Sheet + DB. **Telegram** cancellation marketing line (non-fatal) when configured — see **[[telegram-marketing-reminders]]**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `update-booking-details`             | POST         | property-scoped authenticated JWT               | Strictly allowlisted booking-detail patches, reschedules, asset clears, parking-rate saves, and parking-interest updates. Resolves the booking's property and granular permission leaves server-side, uses the service-role client only after authorization, preserves sanctioned Pending Review reverts with compare-and-swap status checks, and emits `booking.details_edited`. Replaces dashboard PostgREST writes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `create-organization`                | POST         | authenticated JWT                               | Creates org (caller = owner) + optional first property and/or parking slot. **409** if caller already owns a non–hard-rejected org. Hard-rejected orgs (`baseRejectionKind: 'rejected'`) do not block a **new application**. Requires **`contactName`**, **`contactPhone`**; **`contactRole`** from verification rights on finish. **`hostModes`** (`property` \| `parking`). Guide: **[[onboarding]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `upload-org-verification-asset`      | POST         | org owner JWT                                   | FormData **`orgId`**, **`assetType`** (`valid_id` \| `social_proof` \| `selfie_with_id` \| `platform_admin_proof` \| `legitimacy_check_proof` \| `business_permit_bir`; legacy listing types still accepted for backfill). Host Base + Host Recommended only — private bucket **`org-verification-assets`**. Listing proof / Azure PMO → **`upload-listing-authorization-asset`**. **Blocked** when Tier 1 is hard-rejected (`baseRejectionKind: 'rejected'`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `submit-org-verification`            | POST         | org owner JWT                                   | **`orgId`**, **`tier`**: `base` (valid ID + Facebook `social_proof`) or `enhanced` (required selfie with ID; optional platform admin proof and Business permit / BIR). Listing docs → **`submit-listing-authorization`**. Guide: **[[onboarding]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `upload-listing-authorization-asset` | POST         | org owner JWT (listing)                         | FormData **`listingKind`** (`property` \| `parking`), **`listingId`**, **`assetType`** (`proof` \| `additional_proof` \| `azure_pmo_confirmation`). Private bucket **`listing-authorization-assets`**. Guide: **[[onboarding]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `submit-listing-authorization`       | POST         | org owner JWT (listing)                         | **`listingKind`**, **`listingId`**, rights + contract end + proof paths → Tier 1 **`pending`**. **Renew path** for contract expiry/grace cycles (`canSubmitListingRenewal`). Guide: **[[onboarding]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `submit-listing-recommended`         | POST         | org owner JWT (listing)                         | Tier 2 docs (`additional_proof`, `azure_pmo_confirmation`) → **`enhancedStatus = pending`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `get-listing-authorization-assets`   | GET          | org owner or super admin JWT                    | **`?listingKind=&listingId=`** — listing authorization state + signed asset URLs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `list-org-listing-verifications`     | GET          | super admin or org owner JWT                    | **`?orgId=`** — read-only rollup of every property/parking listing's verification status (org modal + super-admin dialog).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `approve-listing-authorization`      | POST         | super admin JWT                                 | **`{ listingKind, listingId }`** — Tier 1 **`pending`** only; activates listing (+ property succession when needed). Guide: **[[approvals]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `approve-listing-recommended`        | POST         | super admin JWT                                 | **`{ listingKind, listingId }`** — Tier 2 **`pending`** only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `reject-listing-authorization`       | POST         | super admin JWT                                 | **`{ listingKind, listingId, tier: 'base' \| 'recommended', kind, reason }`**. Guide: **[[approvals]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `list-organizations`                 | GET          | authenticated JWT                               | Orgs the caller owns, is an active **org-hub member** of (`organization_members` with `ADMIN` or template UUID `role_id`), has active **`property_members`**, or holds a plan-limited inactive seat. **`planLimited: true`** when the caller's only seat in that org is paused. **Super admin** (`SUPER_ADMIN_EMAILS`): all orgs plus a server-derived **`isSuperAdmin: true`** capability used by the client route guard; the allow list never ships in browser JavaScript.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `list-developments`                  | GET          | super admin JWT (`SUPER_ADMIN_EMAILS`)          | All platform **`developments`** rows + linked property/parking counts by **`residence_name`**. Admin UI: **`/admin/developments`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `get-development`                    | GET          | super admin JWT                                 | Query **`?slug=`**. Single development + stats. Admin UI: **`/admin/developments/:slug`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `create-development`                 | POST         | super admin JWT                                 | Body **`{ name, type?, slug?, developerName?, location?, city?, description? }`**. Seeds **`developments`** row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `update-development`                 | PATCH        | super admin JWT                                 | Body **`{ developmentId, …profile fields, settings arrays }`**. Merges **`settings`** JSONB (images, amenities, towers, location extras, marketing fields, **`poolFee`/`poolSchedule`/`guestRequirements`/`guestGuides`/`importantInfo`**, **`announcements[]`**). Validates guest-info text limits via **`developmentGuestInfo.ts`** and announcement shape via **`hostAnnouncements.ts`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `delete-development`                 | POST         | super admin JWT                                 | Body **`{ developmentId }`**. **409** when properties/parkings still reference **`developments.name`** as **`residence_name`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `list-org-verifications`             | GET          | super admin JWT                                 | Org verifications only (`baseStatus ≠ none` or `enhancedStatus ≠ none`). Legacy companion to **`list-super-admin-approvals`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `list-super-admin-approvals`         | GET          | super admin JWT                                 | Unified queue: org verifications + listing verifications + flattened **`app_settings.external_reviews`** rows. **`?type=`**, **`?status=`**, **`?search=`**, **`?organizationId=`** (org hub), **`?page=`**/**`?limit=`**. Queue loading + filter predicates shared via **`_shared/superAdminApprovalsQueue.ts`** (`loadApprovalQueue` / `countPendingApprovals`). Primary source for **`/admin/approvals`** + the org hub **Approvals** section. Guide: **[[approvals]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `moderate-external-review`           | POST         | super admin JWT                                 | **`{ propertyId, reviewId, decision: 'approved' \| 'rejected' }`**. Pending review only; patches JSONB on **`app_settings`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `get-external-review-assets`         | GET          | super admin JWT                                 | **`?propertyId=&reviewId=`**. Screenshot signed URL + stay photo URLs for external review dialog.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `get-org-verification-assets`        | GET          | super admin JWT                                 | **`?orgId=`**. Org + verification state + 1h signed URLs for stored **`org-verification-assets`** paths (review previews).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `approve-org-verification`           | POST         | super admin JWT                                 | **`{ orgId, tier: 'base'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 'enhanced' }`**. Only when that tier is **`pending`**. Sets status **`approved`**, clears rejection reason/kind and **`baseChangesRequestedDocs`**.                                                                                                                                           |
| `reject-org-verification`            | POST         | super admin JWT                                 | **`{ orgId, tier: 'base'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 'enhanced', kind: 'changes'                                                                                                                                                                                                                                                                   | 'rejected', reason, changesRequestedDocs? }`**. Reason required. Only when pending. **`changes`**: soft reject — host keeps shell access; forced non-dismissible resubmit modal on login; optional **`changesRequestedDocs`** (`validId`/`socialProof`/`propertyOwnership`/`parkingProof`) stored as **`baseChangesRequestedDocs`** (empty = show all uploads). **`rejected`**: hard decline — emails owner (`org-verification-rejected.html`), blocks dashboard (`/verification-rejected`), no in-app resubmit (new application). Legacy `compliance`→`changes`. |
| `contract-expiry-cron`               | POST         | cron secret (required hosted)                   | Unit handoff Phase B daily Manila lifecycle (notices, archive, lock, grant expiry). Scheduled by `sync_contract_expiry_cron_job()` at **`0 1 * * *`** UTC. Hosted environments require matching Edge `CONTRACT_EXPIRY_CRON_SECRET` and Vault `contract_expiry_cron_secret`, sent as **`X-Contract-Expiry-Cron-Secret`**; local development remains fail-open when unset.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `get-org-superhost-progress`         | GET          | authenticated JWT + org access                  | Live Superhost criteria snapshot for **Org settings → Trust**. Query **`org_id`** or **`org_slug`**. Returns earned flag, four criterion rows, next assessment date.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `reassess-org-superhost`             | POST         | super admin JWT                                 | **`{ orgId }`** — manual Superhost re-assessment; patches **`organizations.settings.superhost`**, writes **`superhost_assessment_runs`**, sends earn/lose email when status flips.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `superhost-assessment-cron`          | POST         | optional cron secret **or** super-admin JWT     | Quarterly Superhost batch (Jan/Apr/Jul/Oct 1 Asia/Manila; no-op other days). Optional header **`X-Superhost-Assessment-Cron-Secret`** when env set; super-admin manual run from **Org subscriptions**. Snippet: **`supabase/snippets/superhost-assessment-cron.sql`**. Migration: **`20261231130200_superhost_assessment_cron.sql`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `submit-contract-consideration`      | POST         | org owner JWT                                   | **`{ orgId, leg: 'property'\|'parking', note, expectedDate, proofPaths[] }`**. Grace-window self-serve consideration (1×/cycle, ≤14d).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `decide-contract-consideration`      | POST         | super admin JWT                                 | **`{ orgId, leg, decision: 'grant'\|'deny'\|'changes', grantedUntil?, note? }`**. Grant refuses Phase A ACTIVE peer conflicts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `list-hosts`                         | GET          | super admin JWT                                 | Distinct **`organizations.owner_id`** users with profile + org/property/parking counts. Admin UI: **`/admin/hosts`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `get-host`                           | GET          | super admin JWT                                 | Query **`?hostId=`** (auth user UUID). Profile + aggregate stats.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `list-host-organizations`            | GET          | super admin JWT                                 | Query **`?hostId=`**. Orgs owned by host + per-org property/parking counts. UI: **`/admin/hosts/:hostId/orgs`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `list-host-properties`               | GET          | super admin JWT                                 | Query **`?hostId=`**. All properties across host orgs + booking stats + **`organizationSlug`** / **`organizationName`**. UI: **`/admin/hosts/:hostId/orgs/properties`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `list-platform-properties`           | GET          | super admin JWT                                 | All properties across every org + org labels + **`developmentSlug`** / **`developmentName`** (matched from **`residence_name`** → **`developments.name`**). **No booking/revenue/occupancy stats** (privacy). UI: **`/admin/properties`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `update-organization`                | PATCH        | authenticated JWT + owner                       | **`name`** (globally unique display name), **`description`** (max 500), **`tagline`** / **`brandColor`** (stored in **`organizations.settings`** JSONB). When **`name`** changes, **`slug`** is re-derived (globally unique; client cannot set slug).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `setup-guide-state`                  | GET, PATCH   | authenticated JWT + any active org member       | Reads/writes **`organizations.settings.setupGuide`** meta only (`dismissedAt`, `completedAt`, `lastStepId`, `skippedSteps`, `reviewedSteps`, `version`) via RPC **`set_org_setup_guide_state`**. Does **not** require `org.settings.basic:edit`. Atomic merge so org profile/settings saves cannot clobber guide state (and vice versa). UI: `useSetupGuideStateWrite`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `check-organization-name`            | GET          | authenticated JWT                               | Query **`name`**, optional **`excludeOrgId`**. Returns **`{ available, message? }`** for debounced global org display-name validation (org settings + onboarding).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `check-property-name`                | GET          | authenticated JWT                               | Query **`name`**, optional **`excludePropertyId`**. Returns **`{ available, message? }`** for debounced global property display-name validation (property settings + onboarding).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `check-tower-unit`                   | GET          | authenticated JWT                               | Query **`tower`**, **`unitNumber`**, optional **`excludePropertyId`**. Returns **`{ available, message?, conflict? }`** for debounced tower+unit validation (property settings + onboarding). **Today:** any existing row with that pair conflicts. **Planned ([#120](https://github.com/sprmke/kame-homes/issues/120)):** only **`ACTIVE`** rows conflict.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `delete-organization`                | DELETE       | authenticated JWT + owner                       | Body **`{ orgId }`**. Hard-deletes org when **no** `guest_submissions` on any property; best-effort **`property-media`** cleanup from `properties.settings.media`. Cascades org + properties + `org_settings` + per-property settings/integrations. **409** when bookings exist or **RESTRICT** FKs remain (`finance_line_items`, `maintenance_items`). Does **not** remove org logo / app-settings storage. Full matrix: **[[guides/routes/org/settings]]** § Danger zone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `create-property`                    | POST         | authenticated JWT + **`org:properties:create`** | Property within org (org **Owner** / platform admin). **`slug`** auto-generated from **`name`** (globally unique; numeric suffix when taken). Response: `{ property, billingRequired? }` — when org has a live **paid** subscription, `billingRequired: true` and the new property stays off-plan until checkout enrolls all org properties.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `list-properties`                    | GET          | authenticated JWT                               | `?orgId=` or `?orgSlug=`. Owner/platform admin: all org properties. Members: assigned properties (active **or** plan-limited inactive seats). Each property includes **`stats`**. Guide: **[[guides/routes/org/properties]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `copy-property-settings`             | POST         | authenticated JWT + property/org scope          | **Copy:** body **`{ sourcePropertyId, targetPropertyIds, groups, options?, dryRun? }`**. Copies selected configuration groups from one property to others in the **same org** (identity + operational data never copied). Source requires **`settings:view`**; each group on each target checks that group's edit leaf(s) + optional plan feature — skips are reported per target/group. Real runs write **`property_settings_copy_log`**, fire **`property_settings_copied`** notifications, and send a settings-change notice email. **List history:** body **`{ action: 'listLogs', orgSlug                                                                                                                                                                                                                                                                                                                                                                                                                                        | orgId, limit? }`** → `{ logs[] }` after **`verifyOrgAccess`**. Registry: `_shared/propertySettingsClone*.ts`. Guide: **[[guides/routes/org/properties]]**; full inventory: [[workflow/for-testing/property-settings-copy-to-properties]]. **`verify_jwt = false`**; **`serveAuthenticated`**. |
| `update-property`                    | PATCH        | authenticated JWT + owner                       | **`name`** (globally unique display name, case-insensitive), tower/unit (tower validated against selected **residence**), residence, address, maxGuests, status, **`settings`** JSONB merge (contact, description, amenities, **`media`** array with server validation, capacity fields validated per residence defaults, etc.). When **`name`** changes, **`slug`** is re-derived (globally unique; client cannot set slug).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `delete-property`                    | DELETE       | authenticated JWT + owner                       | Body **`{ propertyId }`**. Hard-deletes property when **no** `guest_submissions` rows exist; cleans gallery storage; cascades per-property settings/integrations. **409** when booking history exists (use archive/`INACTIVE` instead).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `create-parking`                     | POST         | authenticated JWT + **`org:parkings:create`**   | Parking slot within org. Validates global slot uniqueness; seeds **`parking_settings`**. **`slug`** auto-generated (globally unique). After create, syncs **`properties.settings.preferredOwnerParkingId`** for org properties with empty/invalid preferred → earliest ACTIVE parking (`created_at` ASC). Guide: **[[guides/routes/org/parkings]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `list-parkings`                      | GET          | authenticated JWT                               | `?orgId=` or `?orgSlug=`. Org owner/admin with **`org:parkings:view`**. Returns parkings + stub stats (zeros).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `update-parking`                     | PATCH        | authenticated JWT + **`org:parkings:manage`**   | Profile fields + **`settings`** JSONB merge; **`status`** `ACTIVE` \| `INACTIVE` (archive/restore). Slot uniqueness re-checked when location fields change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `delete-parking`                     | DELETE       | authenticated JWT + **`org:parkings:manage`**   | Body **`{ parkingId }`**. Cascades **`parking_settings`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `parking-settings`                   | GET, PATCH   | authenticated JWT + parking scope               | Query **`?parking_id=`**. Payment, integrations, notification templates, **`automationToggles`** (email reservation / confirmed / no-host). **Payment PATCH** requires **`settingsVerificationToken`** when **`paymentMethods`** change. Guide: **[[guides/routes/org/parking/settings]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `upload-parking-media`               | POST, DELETE | authenticated JWT + parking scope               | Single cover photo for **`parkings.settings.coverImage`** (max 1). **`property-media`** bucket under **`parking/{id}/`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `upload-parking-settings-asset`      | POST         | authenticated JWT + parking scope               | **`assetType=gcash_qr`** stages Storage only (unique path); **`parking_settings.gcash_qr_image_url`** updates on OTP-gated **`parking-settings` PATCH**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `parking-pricing`                    | GET, PATCH   | authenticated JWT + parking scope               | **GET** weekday/weekend nightly defaults, `dateOverrides`, `bookedDateKeys`, **`blockedDateKeys`**. **PATCH** saves defaults, date overrides, and **`blockRange`** / **`unblockDateKeys`**. Permission **`org:parkings:view`** / **`org:parkings:manage`**. Guide: **[[guides/routes/org/parking/pricing]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `get-public-parking`                 | GET          | anon (public)                                   | **`?parking=<slug>`**. Returns **ACTIVE** parking marketing profile. **404** when missing or **INACTIVE**. Guide: **[[guides/routes/parkings]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `search-suggestions`                 | GET          | anon (public)                                   | Typeahead for marketing search. **`?q=`** (min 2 chars) **`&limit=`**. Grouped locations/developments/properties/parkings; Nearby chip; concept chips (condo/beach/…) with type-first listing previews. Per-IP rate limit (~60/min). Guide: **[[guides/routes/search]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `search-listings`                    | GET          | anon (public)                                   | `/search` results. **`where`**, **`checkIn`/`checkOut`**, **`adults`/`children`/`infants`/`pets`**, **`type`**, **`page`**, **`pageSize`**. Availability exclusion for properties/parkings; developments date-agnostic. Smart intents: Nearby geo (`lat`/`lng`), concept expansion, `meta` payload. Each family uses deterministic candidate ranges with the shared **20k fail-closed ceiling**; pricing/development-count enrichment loads for the **current page only**. Guide: **[[guides/routes/search]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `list-public-properties`             | GET          | anon (public)                                   | `/properties` browse + `/search` property tab filters. Params: `type`, `minPrice`/`maxPrice`, `bedrooms`, `amenities`, `development`, `checkIn`/`checkOut`, `adults`/`children`, `where`, `locationSlug` (place-group filter; same resolver as place-groups), `lat`/`lng`, `sort`, `page`/`pageSize`, map bbox `swLat`/`swLng`/`neLat`/`neLng`. Returns `{ data, total, facets }`. Grid/list facets = after all filters; **map mode** facets = visible bbox pool before categorical filters. Reviews (when not rating-sorted) + superhost enrich **page ids only**. Candidate rows use deterministic PostgREST ranges with a shared **20k fail-closed ceiling**. Guide: **[[guides/routes/properties]]**.                                                                                                                                                                                                                                                                                                                             |
| `list-public-developments`           | GET          | anon (public)                                   | `/developments` browse + `/search` developments tab. Params: `type`, `city`, `minPrice`/`maxPrice`, `developer`, `where`, `locationSlug` (place-group filter; City-normalized), `lat`/`lng`, `sort` (no rating), `page`/`pageSize`, **`slug`** (detail pages; applied on the candidate query). Candidate rows use deterministic PostgREST ranges with a shared **20k fail-closed ceiling**; card `propertyCount` loads for the **current page only**. Guide: **[[guides/routes/developments]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `list-public-parkings`               | GET          | anon (public)                                   | `/parkings` browse + `/search` parkings tab. Params: `location` (legacy `type`), `towers`, `minPrice`/`maxPrice`, `checkIn`/`checkOut`, `where`, `locationSlug` (place-group filter; City-normalized), `lat`/`lng`, `sort`, `page`/`pageSize`, **`developmentSlug`** (resolves the development then filters `residence_name` on the candidate query). Candidate rows use deterministic PostgREST ranges with a shared **20k fail-closed ceiling**. Guide: **[[guides/routes/parkings]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `list-public-place-groups`           | GET          | anon (public)                                   | Unfiltered `/properties`, `/developments`, and `/parkings` location rows. Params: `family`, `groupOffset`, `groupLimit` (default 6, max 12), `previewSize` (default 8, max 12). Returns bounded `{ place, locationSlug, title, total, preview }` groups plus `groupTotal`; only selected previews receive card enrichment. Candidate grouping uses deterministic ranges + the shared **20k fail-closed ceiling**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `list-public-pricing-plans`          | GET          | anon (public)                                   | `/for-hosts/pricing`. Active **subscription** `pricing_plans` (same filter as `org-plan` GET — excludes `commission` and retired `business_plus`). Guide: **[[guides/routes/for-hosts]]**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

`lat`/`lng` on the three `list-public-*` functions scope results to the same Nearby radius (`120 km`) and coordinate resolution `search-listings` uses (`_shared/publicGeoScope.ts`), so a category tab returns the set the All tab already showed. Facets are computed over that scoped set. With map bbox present, facets come from the **visible viewport pool** before type/city/location/price filters so the sidebar tracks what’s on the map. With no explicit `sort`, properties and developments stay nearest-first.

The shared `_shared/publicListingRows.ts` loader pages lean candidates in 1,000-row ranges. If an ACTIVE family exceeds 20,000 candidates, the list/search/place-group endpoint returns an error instead of a truncated page/total/facet response; replace this interim hybrid with SQL/RPC aggregation before raising that ceiling.
| `property-team-members` | GET, PATCH, DELETE | authenticated JWT + property RBAC | **`?property_id=`** or body **`propertyId`**. GET → members (+ virtual org owner). PATCH **`{ memberId, roleId?, permissions?, status? }`**. DELETE **`memberId`**. **`team:view`** / **`team:manage`**. See team route guide. |
| `property-team-invitations` | GET, POST, DELETE | authenticated JWT + property RBAC | Pending invites. POST invite **`{ email, roleId, permissions? }`** (sends Resend email); POST **`action: resend`** + **`invitationId`** (new token + email). **`team:view`** / **`team:invite`**. |
| `property-team-custom-roles` | GET, POST, PATCH, DELETE | authenticated JWT + property RBAC | CRUD **`property_custom_roles`**; PATCH syncs active members + pending invites. **`team:view`** / **`team:manage`**. |
| `property-access` | GET | authenticated JWT | Query **`property_id`**. Returns caller **`accessKind`**, **`permissions[]`**, slugs for UI guards. |
| `parking-team-members` | GET, PATCH, DELETE | authenticated JWT + parking RBAC | **`?parking_id=`** or body **`parkingId`**. GET → members (+ virtual org owner/admins). PATCH **`{ memberId, roleId?, permissions?, status? }`**. DELETE **`memberId`**. Built-in roles **MANAGER** / **STAFF** / **VIEWER** or custom role UUID. **`team:view`** / **`team:manage`**. |
| `parking-team-invitations` | GET, POST, DELETE | authenticated JWT + parking RBAC | Pending invites. POST **`{ email, roleId, permissions? }`** (Resend email, **`scope=parking`** accept link); POST **`action: resend`**. **`team:view`** / **`team:invite`**. |
| `parking-team-custom-roles` | GET, POST, PATCH, DELETE | authenticated JWT + parking RBAC | CRUD **`parking_custom_roles`**; PATCH syncs active members + pending invites. **`team:view`** / **`team:manage`**. |
| `parking-access` | GET | authenticated JWT | Query **`parking_id`**. Returns caller **`accessKind`**, **`permissions[]`**, org/parking slugs for UI guards. |
| `accept-parking-invite` | POST | authenticated JWT | Body **`{ token }`**. Upserts **`parking_members`**. Returns **`orgSlug`**, **`parkingSlug`**, **`parkingName`**. |
| `org-access` | GET | authenticated JWT | Query **`org_id`** or **`org_slug`**. Owner, org-hub member (`ADMIN` or template UUID), property member, platform admin, or **super admin**. Returns **`accessKind`**, **`permissions[]`**, org slugs, team/properties/settings flags for UI guards. |
| `get-team-invite-preview` | GET | public (`verify_jwt = false`) | Query **`?token=`** (+ optional **`scope=org                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | property                                                                                                                                            | parking`**). Pending invite only. Returns org name, logo URL, invite email, role; property invites also **`propertyName`**, **`unitLabel`**, **`propertyLocation`**; parking invites also **`parkingName`**, **`parkingLocation`**. |
| `accept-property-invite` | POST | authenticated JWT | Body **`{ token }`**. Invitee email must match invitation; upserts **`property_members`**. Returns **`orgSlug`**, **`propertySlug`**, **`propertyName`** for SPA redirect. |
| `org-team-members` | GET, PATCH, DELETE | authenticated JWT + property/parking team | **`?org_id=`** / **`?org_slug=`** or body **`orgId`**. GET → members (+ virtual owner). PATCH **`{ memberId, status? }`**. DELETE **`memberId`**. Owner / org ADMIN / platform admin. |
| `org-team-invitations` | GET, POST, DELETE | authenticated JWT + property/parking team | Pending org invites. POST **`{ email, roleId: 'ADMIN' }`** (Resend email); POST **`action: resend`**. |
| `accept-org-invite` | POST | authenticated JWT | Body **`{ token }`**. Upserts **`organization_members`**. Returns **`orgSlug`**, **`orgName`**. |
| `meta-inbox-oauth-start` | POST | authenticated JWT + property/parking | Returns Meta OAuth URL; CSRF state in **`meta_inbox_oauth_state`**. Requires **`inbox:manage`** + `property_id`/`parking_id`. |
| `meta-inbox-oauth-callback` | GET | public redirect | Meta redirect; persists Page token + triggers async backfill; single Page auto-connects; multiple Pages → picker session + redirect **`meta_inbox=select_page`**. |
| `meta-inbox-oauth-pages` | GET | authenticated JWT + property/parking | List pending Facebook Pages for picker (`?picker=` state). Auth via OAuth state scope + **`inbox:manage`**. |
| `meta-inbox-oauth-complete` | POST | authenticated JWT + property/parking | Connect selected Page; triggers async backfill. **`inbox:manage`**. |
| `meta-inbox-backfill` | POST | service role or JWT + org | Chunked Graph sync (`phase`, `nextUrl`, `light`); one conversations page per call. JWT: **`inbox:manage`** (full) or **`inbox:view`** when `light: true` (scroll sync). |
| `meta-inbox-status` | GET | authenticated JWT + property/parking | Meta connection cards; **`metaHasMore`** plus webhook verification state (`webhookNeedsAttention`, last verify, attempts). **`inbox:view`**. |
| `meta-inbox-disconnect` | POST | authenticated JWT + property/parking | Disconnect Meta (Facebook + linked Instagram) and remove synced conversations/messages for this scope. **`inbox:manage`**. |
| `meta-inbox-resubscribe` | POST | authenticated JWT + property/parking | Re-verify the effective Meta Page webhook and re-subscribe it without deleting any inbox data. **`inbox:manage`**. |
| `meta-inbox-webhook-healthcheck` | POST | cron / internal | Hosted `pg_cron` + `pg_net` health check for connected Facebook Pages; re-subscribes dropped webhooks and increments retry state. Optional `X-Meta-Inbox-Webhook-Healthcheck-Cron-Secret`. |
| `meta-inbox-webhook` | GET, POST | public (signed POST) | Meta webhook verify + ingest (Messenger / Instagram DMs, read receipts). Dedupe **`social_webhook_events`**. |
| `social-inbox-threads` | GET | authenticated JWT + property/parking | **DB-only** filter/paginate **`social_conversations`**; returns **`metaHasMore`**. Query **`type`**, **`status`**, **`platform`**, **`search`**, **`cursor`**. Route guide: **[[guides/routes/org/property/inbox]]**. |
| `social-inbox-messages` | GET, POST, PATCH | authenticated JWT + property/parking | GET messages; POST `mark_read` or `{ action: 'unsend', messageId }`; PATCH edit host outbound until guest read. **`inbox:view`** / **`inbox:reply`**. |
| `social-inbox-send` | POST | authenticated JWT + property/parking | Reply to DM; optional **`replyToMessageId`** (Meta DMs pass **`reply_to.mid`**). Web branch accepts **`attachments`**; sends offline guest email via **`guestChatEmail.ts`** when guest has not read. Enforces Meta 24h window by default, with explicit **`useHumanAgentTag: true`** support for 24h-7d non-promotional follow-ups; still skipped for `platform=web`. **`inbox:reply`**. |
| `social-inbox-templates` | GET, POST, PATCH, DELETE | authenticated JWT + property/parking | Quick reply CRUD on **`social_reply_templates`**. **`inbox:manage`**. |
| `social-inbox-ai-suggest` | POST | authenticated JWT + property/parking | AI draft reply (Gemini/Groq) with guest-safe property/org grounding + output guard. Returns `{ suggestion, flagged }`. **429 + `upgradeHook`** when org quota exceeded. **`inbox:reply`**. |
| `ai-platform-global-settings` | GET/PATCH | super-admin | Platform AI kill switch + `enforceQuotas` + `allowedFeatures` allowlist + default daily/monthly/cost limits + **`creditUnitUsd`** + **`voiceReceptionistCostPerMinuteUsd`**. |
| `ai-platform-settings` | GET/PATCH | authenticated JWT + org | Per-org AI enable + daily/monthly call + daily USD cost limits. `org:settings:view` / `org:settings:edit`. |
| `ai-platform-usage` | GET | authenticated JWT + org | Org AI usage summary (calls, estimated USD, **`monthCreditsConsumed`**/**`monthlyCreditLimit`**, **`walletBalanceCredits`**, remaining quota) + per-feature + per-property breakdowns. `org:dashboard:view`. |
| `ai-platform-property-settings` | GET/PATCH | authenticated JWT + property | Per-property AI enable + optional daily/monthly/cost overrides (NULL = inherit org) + **image/video monthly credit caps** (`imageMonthlyCreditCap` / `videoMonthlyCreditCap`, blank = 60% of org allowance). Hosts cannot flip premium. `settings:view` / `settings.aiOverrides:edit`. |
| `ai-platform-credit-wallet` | GET, POST | super-admin | **`?org_id=`** or **`?org_slug=`** — GET returns purchased top-up **`balanceCredits`** + recent **`ai_platform_org_credit_ledger`** rows. POST **`{ creditsDelta, description? }`** → **`adjustOrgCreditWallet()`** (`manual_adjustment`). Wallet draw-down enforcement is **live** (Phase 3 shipped) — `assertOrgAndPropertyAiQuota` checks the wallet once an org's/property's daily or monthly credit allowance is exceeded, `recordAiUsage` debits it atomically via `adjust_ai_platform_org_credit_wallet` — see **[[ai-usage-metering-credits-foundation]]**. UI: **`AiCreditWalletCard`** on **`/admin/settings`** and the org hub **AI credits** section. |
| `ai-platform-generation-overrides` | GET, PATCH | super-admin (OTP on PATCH) | Per-property Generate sub-caps and premium-tier hatch. GET **`?org_id=`** / **`?org_slug=`** → properties + `imageMonthlyCreditCap` / `videoMonthlyCreditCap` / `allowPremiumImage` / `allowPremiumVideo`. PATCH **`{ propertyId, imageMonthlyCreditCap?, videoMonthlyCreditCap?, allowPremiumImage?, allowPremiumVideo? }`**. Caps are also editable by hosts via `ai-platform-property-settings`; **only this endpoint** can set `allow_premium_tier`. Step-up action `ai_generation_overrides`. UI: org hub **AI credits**. |
| `pricing-plans` | GET, POST, PATCH | super-admin | CRUD **subscription** **`pricing_plans`** catalog (commission excluded). GET list or **`?planId=`**; PATCH name/tagline/sort/price/discount/features/active. UI: **`/admin/pricing/plans`**. |
| `platform-payment-settings` | GET, PUT | super-admin | Singleton **`platform_payment_settings`** — enabled PayMongo rails (ops reference; rails are enabled on the PayMongo account/dashboard), grace period, renewal lead days. UI: **`/admin/pricing/payment-settings`**. |
| `platform-billing-cron` | POST | cron secret (optional) | Daily dunning sweep over **`org_subscriptions`**: renewal links, **`past_due`** / **`suspended`** transitions, pooled team-seat clawback, billing emails. **`serveCronPost`**. Manual: super-admin **Run billing cron** or **`org-subscriptions-admin`** PATCH **`run_billing_cron`**. |
| `org-subscriptions-admin` | GET, POST, PATCH | super-admin | GET list organizations + current subscription (**`?search=`**, **`?planCode=`**, **`?limit=`**) or **`?organizationId=`** detail + transactions/events. POST manual assign/change **`{ organizationId, planId, propertyIds?, overridePricePhp? }`**. PATCH **`run_billing_cron`** or **`extend_period`** (support override). UI: **`/admin/pricing/subscriptions`** + org hub **Subscription** section. |
| `super-admin-overview` | GET | super-admin | Platform dashboard for **`/admin`**. **`?range=30d\|90d\|12mo`**. Returns `kpis` (orgs / hosts / properties / parkings / live subs + MRR / open tickets / pending approvals / undisbursed parking payouts / AI spend), `growthSeries` (12-mo new + cumulative orgs & subscriptions), `planMix`, `aiCostByFeature` (from `ai_platform_usage_events`), `attention`, and `recent` (orgs / subscriptions / tickets). All counts platform-wide + exact. |
| `list-organizations-admin` | GET | super-admin | Organization directory for **`/admin/orgs`**. **`?q=`**, **`?plan=<code>\|none`**, **`?page=`**, **`?limit=`**; **`?summary=true`** → counts. Each row: plan, subscription status, MRR, property + parking counts. Plan assignment stays in `org-subscriptions-admin`. |
| `super-admin-ai-usage` | GET | super-admin | Platform AI cost console for **`/admin/ai-usage`**. **`?range=30d\|90d\|12mo`** → daily spend/calls trend, cost-by-feature, top-25 orgs by spend with today/month calls vs. `ai_platform_org_settings` limits, and `quotaBreaches`. |
| `list-super-admin-audit` | GET | super-admin | Super-admin action log for **`/admin/audit`** + org-hub **Activity**. **`?q=`**, **`?actor=`**, **`?targetType=`**, **`?targetId=`**, **`?page=`**, **`?limit=`**. Reads `super_admin_audit_events`, written by `_shared/superAdminAudit.ts#logSuperAdminAction()`. |
| `list-activity-log` | GET | authenticated JWT + org (`verifyOrgAccess`) | Org activity / audit timeline. **`?orgId=`** or **`?orgSlug=`** (required); filters **`?scope=`**, **`?propertyId=`**, **`?parkingId=`**, **`?category=`** (comma list), **`?actorUserId=`**, **`?action=`**, **`?severity=`**, **`?targetType=`**, **`?targetId=`**, **`?q=`**, **`?dateFrom=`** / **`?dateTo=`** (ISO); **keyset** cursor **`?cursorTs=`** + **`?cursorId=`**, **`?limit=`** 1–100 (default 50), default 90-day window. Owner / org-admin / platform-admin see every row; listing-scoped members see only rows for their assigned property / parking ids (never `scope='org'`). Reads `activity_log`, written by `_shared/activityLog.ts`. Powers the Activity routes + `<EntityActivityHistory>` (`?targetType=&targetId=`). |
| `activity-log-export` | GET | authenticated JWT + org (`verifyOrgAccess`) | CSV export of the activity timeline for the current filter (`?orgId=`/`?orgSlug=` + `?scope=`, `?category=`, `?severity=`, `?propertyId=`, `?parkingId=`, `?dateFrom=`/`?dateTo=`). **Owner / org-admin / platform-admin only** (listing-scoped members → 403 — the export is a whole-org artifact). **Plan gate:** `requireOrgFeature(org.id, 'activityLogExport')` (Starter+) — non-platform callers on Free get the `jsonUpgradeHook` envelope; platform admins bypass. Streams `text/csv` with `Content-Disposition: attachment`; bounded (default 90-day window, max 20k rows). Same `activity_log` source. |
| `platform-settings` | GET, PUT | super-admin | Singleton **`platform_settings`** — signups on/off, maintenance mode + message, default plan code, support email, legal URLs, public rate-limit ceiling. UI: **`/admin/platform-settings`**. No RLS read path. |
| `super-admin-search` | GET | super-admin | ⌘K palette source. **`?q=`** (min 2 chars) fans out over orgs/properties/parkings/support tickets (bounded `ilike` + small per-source limit) → grouped `results[]` with `href`. |
| `get-organization-admin` | GET | super-admin | One org for the **`/admin/orgs/:slug`** hub. **`?slug=`** or **`?organizationId=`**. Returns identity + owner (Auth Admin API) + property/parking/member counts + live plan + verification (`readOrgVerificationFromSettings`) + open work (pending approvals + open tickets, org-scoped via `countPendingApprovals(orgId)`). |
| `org-plan` | GET | authenticated JWT + org (`verifyOrgAccess`) | **`?orgId=`** or **`?orgSlug=`** — every active subscription plan (with `volumeDiscountTiers`), the org's properties, current subscription + enrolled property ids, transactions, **`pendingCheckoutUrl`**. UI: **`/org/:orgSlug/plans`**. |
| `create-org-subscription-checkout` | POST | authenticated JWT + org owner | **`{ organizationId, planId }`** — PayMongo Payment Link priced at rate × **every org property** × volume discount; pending **`org_payment_transactions`** row; returns **`checkoutUrl`**. Subscription checkout for first purchase, **upgrade**, and property-count changes. Requires **`PAYMONGO_SECRET_KEY`**. |
| `apply-org-plan-downgrade` | POST | authenticated JWT + org owner | **`{ organizationId, planId }`** → **`{ orgSubscriptionId, toFree }`**. Immediate self-serve **downgrade** (paid→lower paid via `changeOrgSubscription` for **all org properties**, or paid→Free by unenrolling then canceling). Rejects **`past_due`**, **Managed**, upgrades, and same-tier; **`suspended` → Free only**. Expires pending checkouts; emails owner via `sendOrgSubscriptionPlanChangedEmail`. No PayMongo. |
| `paymongo-webhook` | POST | public (signature) | Verifies **`Paymongo-Signature`**, dedupes **`processed_paymongo_events`**, fulfills the org subscription on **`payment.paid`** / **`link.payment.paid`** via **`subscriptionOrchestrator`**. **`verify_jwt = false`**. |
| `property-entitlements` | GET | authenticated JWT + property | **`?property_id=`** — returns the full **`resolvePropertyEntitlements(propertyId)`** object (every **`PlanFeatures`** key + plan metadata; do not omit keys — missing fields make client gates treat paid features as off). **`settings:view`**. Consumed by **`usePropertyEntitlements`** / feature gates across the dashboard. |
| Plan-gated PATCH/POST (429 + `upgradeHook`) | — | property/org scoped | See **`planEntitlements.ts`**: `telegram-*-settings` (enable), `property-team-invitations` (invite), `social-inbox-settings` (auto-send), `dashboard-assistant-chat`, `booking-ai-review`, `validate-booking-receipts`, `voice-receptionist-settings` / `voice-receptionist-start`, `submit-org-verification` (base + enhanced), `submit-listing-recommended`, `publish-to-meta`. Helpers: **`requirePropertyFeature`**, **`requireOrgPropertyFeature`**, **`requireTeamInviteAllowed`**, **`catchPlanFeatureError`**. Runtime: **`metaInboxAutoReply`**, **`webInboxAutoReply`**. |
| `social-inbox-settings` | GET, PATCH | authenticated JWT + property/parking | Automation toggles on **`social_inbox_settings`**. **`inbox:manage`**. |
| `notifications-list` | GET | authenticated JWT + org/property/parking | Keyset-paginated **`notifications`** for the caller's org (`?cursor`, `?limit`, default 20/max 50) + capped (100) **`unreadCount`**. Inbox rows collapse to **one per `conversation_id`** in both the list and the badge count (select includes `type` + `conversation_id` for the count query). **`org:dashboard:view`** (org scope) or **`bookings:view`** (property/parking scope) — not **`notifications:view`**, which gates the Telegram settings pages below. |
| `notifications-mark-read` | POST | authenticated JWT + org/property/parking | `{ notificationId }` (single) or `{ markAll: true }` (optionally narrowed to the resolved property/parking scope, capped at 500). Upserts **`notification_reads`** `(notification_id, user_id)`, `ignoreDuplicates: true`. Same access gate as `notifications-list`. |
| `push-subscribe` | POST | authenticated JWT (`serveAuthenticated`) | `{ subscription: { endpoint, keys: { p256dh, auth } }, userAgent?, platform? }` → upsert **`push_subscriptions`** on `endpoint` for the caller (`failure_count = 0`, `disabled_at = null`, `last_seen_at = now()`). PWA Web Push opt-in. See **[[pwa]]** §5. |
| `push-unsubscribe` | POST | authenticated JWT | `{ endpoint }` → delete the caller's matching **`push_subscriptions`** row. |
| `push-fanout` | POST | secret (`X-Push-Fanout-Secret`), `verify_jwt = false` | `{ notificationId }`. Called by the `trg_notifications_push_fanout_ins` / `_upd` triggers via `pg_net` on a `notifications` INSERT or coalesced UPDATE. Resolves recipients (org owner + active `organization_members`, `_shared/pushRecipients.ts`), loads active `push_subscriptions`, sends Web Push via `_shared/webPushService.ts` (`@negrel/webpush`, concurrency 20), prunes 404/410, bumps `failure_count` on 429/5xx. Non-fatal end to end; logs `[push-fanout] {sent,pruned,failed,...}`. |
| `list-help-center-articles` | GET | authenticated JWT | Groups **`ai_dashboard_assistant_knowledge_base`** into per-guide articles (`module`, `title`, `routePath`, `qaItems[]`). Excludes **`docs/guides/routes/admin/**`**. Populated by **`bun run sync:ai-knowledge-base`** (+ CI on route-guide merges). Guide: **[[guides/routes/org/property/help-support]]**. |
| `list-help-center-faqs` | GET | authenticated JWT | Published **`help_center_faqs`** rows only. Guide: **[[guides/routes/org/property/help-support]]**. |
| `list-host-announcements` | GET | authenticated JWT + org/property/parking scope | Query **`?orgId=`** and optional **`?propertyId=`** / **`?parkingId=`**. Returns live **platform** announcements (`platform_host_settings.announcements`) plus **development** announcements matched from **`properties.residence_name`** / **`parkings.residence_name`** → **`developments.name`**. Merged + sorted (critical → warning → info). Super-admin authoring: **`/admin/announcements`** (platform) and **`PATCH update-development`** (development). Host UI: org **Announcements** archive (sidebar; unread red-dot); property/parking `/announcements` deep-link fallbacks. **`verify_jwt = false`**; **`serveAuthenticated`**. |
| `get-platform-host-settings` | GET | super admin JWT | Singleton **`platform_host_settings`**. UI: **`/admin/announcements`**. |
| `update-platform-host-settings` | PATCH | super admin JWT | Body **`{ announcements[] }`**. Validates + stamps **`updatedAt`** only when content changes. |
| `submit-support-ticket` | POST | authenticated JWT; host org/property/parking **or** guest (no org) | Creates **`support_tickets`** + first message (`sender_type` host/guest); notify **`SUPPORT_TEAM_EMAIL`**. |
| `list-support-tickets` | GET | authenticated JWT + scope | Caller's tickets — host channel filtered by org, or guest channel for explore. |
| `get-support-ticket` | GET | authenticated JWT + scope | Ticket + reply thread (guest: submitter only). |
| `reply-support-ticket` | POST | authenticated JWT + scope | Host/guest reply; **resolved** → **`in_progress`**; **closed** → **409** (use reopen). |
| `reopen-support-ticket` | POST | authenticated JWT + scope | Submitter reopens a **closed** ticket → **`open`**. |
| `upload-support-ticket-attachment` | POST | authenticated JWT + scope | Multipart → private **`support-ticket-attachments`** (images + `video/mp4`/`video/quicktime`, 20 MB, max 3 per compose). |
| `list-support-tickets-admin` | GET | super-admin | All tickets incl. explore guest; search + status/category filters. Guide: **[[admin/support]]**. |
| `get-support-ticket-admin` | GET | super-admin | Full thread for triage dialog (org optional). |
| `reply-support-ticket-admin` | POST | super-admin | Admin reply; **open/resolved/closed** status transitions per `_shared/supportTicketStatus.ts`; **`sendSupportTicketReplyNotify`** to submitter (host or guest URL). |
| `update-support-ticket-status` | POST | super-admin | Status/priority update without a new message. |
| `list-help-center-faqs-admin` | GET | super-admin | All FAQ rows including unpublished. |
| `create-help-center-faq` | POST | super-admin | New **`help_center_faqs`** row. |
| `update-help-center-faq` | PATCH | super-admin | Edit fields; **`sortOrder`** handles reordering (no separate reorder endpoint). |
| `delete-help-center-faq` | DELETE | super-admin | **`?id=`** — remove FAQ row. |
| `guest-web-chat-start` | POST | authenticated JWT (guest) | Create **`platform=web`** thread for **`propertySlug`** or **`parkingSlug`** (mutually exclusive) + inquiry dates (first inquiry). Upserts **`social_conversations`** with `property_id` or `parking_id`. Not admin allow-list. Property responses include **`voiceReceptionistEnabled`**; parking threads omit voice receptionist. |
| `guest-web-chat-resume` | GET | authenticated JWT (guest) | `?property_slug=` or `?parking_slug=` — returns existing web thread when guest already has messages (`hasMessages`, `conversationId`, inquiry dates). Property responses include **`voiceReceptionistEnabled`**. |
| `guest-web-chat-messages` | GET, POST, PATCH | authenticated JWT (guest) | GET paginated messages; POST send (text and/or **`attachments`**), **`mark_read`**, or **`unsend`**; PATCH edit own inbound until read/host reply. Auto-reply via **`webInboxAutoReply`** when automation enabled. |
| `upload-guest-chat-asset` | POST | authenticated JWT (guest) | Multipart upload → Storage **`guest-chat-attachments`** (JPEG/PNG/WebP/PDF, 10 MB); returns attachment object for **`guest-web-chat-messages`** send. |
| `upload-inbox-chat-asset` | POST | authenticated JWT (host, `inbox.messages:edit`) | Same bucket/limits as guest upload; **`conversationId`** + property/parking scope; web chat host replies only. |
| `voice-receptionist-start` | POST | authenticated JWT (guest) | AI voice receptionist. Checks platform AI kill switch + `voice_receptionist` in `allowed_features` + per-property `ai_platform_property_settings.enabled`/`feature_configs.voice_receptionist.enabled`, enforces per-guest daily + property concurrent session caps (**`voice_receptionist_sessions`**), attaches to the guest's existing **`platform=web`** thread, builds guest-safe grounding via **`inboxAiGuestContext.ts`**, and mints a locked Gemini Live ephemeral token (**`geminiLiveEphemeral.ts`**). Returns `{ ephemeralToken, sessionId, model, voiceId, maxSessionSeconds }`. |
| `voice-receptionist-tool` | POST | authenticated JWT (guest) | Serves the `getPropertyFact(topic)` tool call relayed from the browser during an active voice session — the hard security boundary. Topic allowlist mirrors the guest-safe fields from **`inboxAiGuestContext.ts`**; unmatched topics (finance, other guests, staff/internal ops) are rejected with 400. |
| `voice-receptionist-end` | POST | authenticated JWT (guest) | Ends a voice session (`{ sessionId, endReason?, transcript? }`) — sets `ended_at`/`duration_seconds`/`end_reason` on **`voice_receptionist_sessions`**, records estimated cost in **`ai_platform_usage_events`** (feature `voice_receptionist`), runs **one** batch Gemini Flash polish of the transcript (**`polishVoiceUtterance.ts`**), then batch-writes into the guest's `social_messages` thread with **`source_mode='voice'`** (idempotent per session). |
| `voice-receptionist-settings` | GET, PATCH | authenticated JWT + property | Per-property voice receptionist config (now stored in **`ai_platform_property_settings.feature_configs.voice_receptionist`**) — enable, voice, persona prompt, session caps (**60–3600** s / **1–999** daily / **1–50** concurrent). **`settings:view`** / **`settings:edit`**. |
| `voice-receptionist-voice-preview` | POST | authenticated JWT + property | Short Gemini TTS sample for the selected Live prebuilt voice (`{ voiceId, propertyName? }` → PCM base64). Uses Basic Information **property name** (`properties.name` or body override); never tower + unit. Durable RL (20/hour/user) + `assertOrgAndPropertyAiQuota` + `recordAiUsage` (`voice_receptionist`). **`settings.voiceReceptionist:edit`**. |
| `voice-receptionist-usage` | GET | authenticated JWT + property | Read-only usage/cost summary (last 30 days) from **`voice_receptionist_sessions`** — session counts, avg duration, rough estimated cost. **`settings:view`**. |
| `ai-platform-property-settings` | GET, PATCH | authenticated JWT + property | Per-property AI overrides: enable toggle, optional daily/monthly call and daily USD cost limits (NULL = inherit org), plus **image/video monthly credit caps** in `feature_configs`. Hosts cannot set Premium. **`settings:view`** / **`settings.aiOverrides:edit`**. |
| `dashboard-assistant` | POST | authenticated JWT + org | AI ops assistant (Phase-B plain-text stub — pre-dates the full assistant below). `org:dashboard:view`. Classifies risk, builds RBAC-scoped org context, calls Gemini, applies safety guard. |
| `dashboard-assistant-chat` | POST | authenticated JWT + org | Full AI dashboard assistant turn: kill-switch + quota checks, permission-scoped grounding facts, bounded (4-round) Gemini tool-calling loop against the fixed tool catalog (`_shared/dashboardAssistantTools.ts`), structured `ChatBlock[]` synthesis + grounding/safety guard, Tier-1 auto-execute with audit logging, Tier-2 short-circuits into a persisted `ai_dashboard_assistant_pending_actions` proposal. Body: `{ conversationId?, orgSlug, pageContext: { propertyId?, parkingId?, bookingId? }, attachedContext?: AttachedContextItem[], message, attachments?: [{ name, mimeType, dataBase64 }], stream?: boolean, regenerate?: boolean }`. Message or attachments required (except regenerate). **`regenerate: true`** requires `conversationId`, deletes assistant rows after the last user message, reuses that user row (no duplicate), rejects attachment turns. **Streaming:** send `Accept: text/event-stream` or `stream: true` — SSE JSON events (`phase`, `tool_start`, `tool_done`, `plan`, `plan_update`, `text_start`, `text_chunk`, terminal `blocks` / `error`) via `_shared/dashboardAssistantStreamEvents.ts`; UI client `ui/.../assistantStream.ts`. `text_*` events chunk the first `text` block (~18ms spacing) before `blocks`. Client disconnect / stream `cancel` aborts the turn; Tier-1 writes queue during the tool loop and commit only after synthesis + safety (`_shared/dashboardAssistantDeferredWrites.ts`) — cancel before commit discards the queue and deletes the uncommitted user message; mid-commit abort audits partial writes and returns `error.appliedEffects`. Non-stream JSON unchanged. **`aiDashboardAssistant`** plan gate applies on property routes; **skipped on parking-only routes** (interim, until org plans). Files (JPEG/PNG/WebP/PDF, max 3 × 4 MB) go to Storage **`ai-assistant-attachments`**; Postgres stores metadata only. `pageContext` is the current route; `attachedContext` is the host's explicit composer pins (validated + RBAC). Pins are preferred for matching tools (`get_booking` / transitions) before falling back to `pageContext`. File requests render a grounded `file_list` (storage URLs from the booking Files tab; UI signs private buckets). |
| `dashboard-assistant-confirm` | POST | authenticated JWT | Executes or denies a Tier-2 proposal from `dashboard-assistant-chat`. Body: `{ actionId, confirm }`. Only the proposing user may confirm; re-derives risk tier from current DB state before executing (`assertActionSafeToExecute`); idempotent on repeat calls (`alreadyResolved: true`). |
| `dashboard-assistant-conversations` | GET, DELETE | authenticated JWT + org (list) | List the caller's own AI assistant conversations (`?org_slug=`), load one thread (`GET ?conversation_id=`), or delete one the caller owns (`DELETE ?conversation_id=`). Private per user. Delete cascades messages/pending actions and best-effort removes Storage objects under `{orgId}/{userId}/{conversationId}/`. |
| `dashboard-assistant-settings` | GET, PATCH | authenticated JWT + org | Org opt-in + per-property disable list + message/write-action quotas for the AI dashboard assistant. GET has no permission gate (any member needs it to know whether the launcher should render) and includes `platformEnabled`; opt-in `?includeUsage=true` (used only by the settings page, not the hot launcher-visibility check) also returns this-month message/write-action/**`creditsConsumed`** usage, reconciled with `ai_platform_usage_events` for `feature='dashboard_assistant'`. PATCH requires `org:settings:edit`. Independent of `ai-platform-settings`. |
| `dashboard-assistant-global-settings` | GET, PATCH | super-admin | Platform-wide kill switch for the AI dashboard assistant. Independent of `ai-platform-global-settings`. |
| `dashboard-assistant-expire-pending-actions` | POST | cron secret | TTL sweep — flips stale `pending` rows in `ai_dashboard_assistant_pending_actions` to `expired` (15 min TTL). Scheduled via `public.sync_dashboard_assistant_expire_cron_job()` (migration `20261316121100_dashboard_assistant_expire_cron.sql`, every 5 min). Optional `DASHBOARD_ASSISTANT_EXPIRE_CRON_SECRET` / header `X-Dashboard-Assistant-Expire-Cron-Secret`. |
| `get-booking` | GET | authenticated JWT + property/org | Minimal single-booking read (fills the gap `list-bookings` didn't cover) — `?booking_id=` + `?property_id=` or `?org_id=`/`?org_slug=`. Returns the `booking_card`-shaped fields. `bookings:view` / `org:bookings:view`. |
| `get-booking-ai-assistant-audit` | GET | authenticated JWT + property | Read-only "Actions taken by AI assistant" trail for a booking from `ai_dashboard_assistant_action_audit`. `?booking_id=`. `bookings:view`. |
| `guest-profile` | GET, PATCH | authenticated JWT (guest) | Read/update **`guest_profiles`** for **`auth.uid()`**; links legacy bookings by email on GET. Server uses **`service_role`** (table grant required — see migration `20261127120000_guest_profiles_service_role_grants.sql`). |
| `upload-guest-profile-asset` | POST | authenticated JWT (guest) | Multipart avatar → **`guest-profile-assets`** bucket; updates profile row. |
| `guest-trips` | GET | authenticated JWT (guest) | Paginated guest bookings from **`guest_submissions`** (`guest_user_id` or email fallback). Not wired in guest account UI after Stays became the messaging hub; kept for possible reuse. |
| `list-guest-vouchers` | GET | authenticated JWT (guest) | Next-stay voucher wallet. Optional `property` / `propertyId` filter; `includeRedeemed=1` for history. Links bookings by email first. |
| `guest-messages` | GET | authenticated JWT (guest) | Web chat thread list for **`social_conversations.guest_user_id`**. |
| `marketing-templates` | GET, POST, PATCH, DELETE | authenticated JWT + property | **CRUD** property-scoped marketing design templates on **`marketing_templates`** (`contentType`: `calendar` \| `design` \| `video`; `designJson`, `platform`, `aspectPreset`). **`?property_id=`**. **`verify_jwt = false`**; **`serveAuthenticated`**. GET **`marketing:view`** + `marketingStudio`; POST **`marketing.templates:add`** + `customTemplates`; PATCH **`marketing.templates:edit`** + `customTemplates`; DELETE **`marketing.templates:delete`** only. |
| `list-property-guest-reviews` | GET | authenticated JWT + property | Marketing Studio review picker — merged newest-first **Kame** `guest_reviews` + approved `app_settings.external_reviews`. Query **`?property_id=`**, optional **`minRating=`**, **`limit=`**, **`source=kame\|facebook\|airbnb\|all`**, **`socialSeedOnly=1`**. Each row includes **`socialSeedAllowed`** (false for Airbnb text). **`marketing:view`** only (no plan gate — preview-open). **`verify_jwt = false`**; **`serveAuthenticated`**. |
| `publish-to-meta` | GET, POST | authenticated JWT + property | **GET** lists **`marketing_publications`**. **POST** publish to Meta: **`{ connectionId, publishType \| platform+postType, imageUrl/mediaUrl, mediaType?, caption?, scheduledAt? }`** — **data URLs** uploaded to **`property-media`** first. FB photo post; IG feed/story/reel (video). Logs **`marketing_publications`**. **`?property_id=`**. **`serveAuthenticated`**. GET **`marketing:view`** + `marketingStudio`; POST **`marketing.publish:add`** + **`marketingPublishLimitPerGroup`** (Business+; `requireMarketingPublishAllowed`) + publish-limit check. |
| `generate-marketing-caption` | POST | authenticated JWT + property | AI caption: **`{ platform, postType, contentHint?, nightlyRate?, availabilityText? }`** → **`{ caption }`** (Gemini/Groq). **`?property_id=`**. **`serveAuthenticated`**. **`marketing.generate:add`** + `aiMarketingGeneration`. |
| `generate-marketing-template` | POST | authenticated JWT + property | AI design tokens for Content Studio: **`{ contentType: calendar\|design\|video, prompt, includeContext?, amenitiesText?, availabilityText?, preferences? }`**. For `design`: `preferences.category` (required), `preferences.content` (overrides category default), `preferences.includeContext { propertyPhoto?, orgLogo?, propertyName?, cta? }`, `preferences.ctaLabel?`, `preferences.photoSearchQuery?`. Returns **`{ contentType, tokens }`**; client compiles into Polotno design templates. Calendar MVP compiles tokens client-side into CalendarStyles and saves custom templates for all three canvas formats. Gemini/Groq. **`?property_id=`**. **`serveAuthenticated`**. **`marketing.generate:add`** + `aiMarketingGeneration`. Video content type not implemented yet. |
| `marketing-music` | GET, POST | authenticated JWT + property | **GET** Jamendo browse/search (`?q=`, `?order=popularity_week` \| `popularity_total` \| `relevance`) — requires **`JAMENDO_CLIENT_ID`**; returns `{ tracks, jamendoConfigured }`. **POST** JSON **`import-jamendo`** \| **`import-url`** or multipart **`file`** — caches audio to **`property-media`** at `{propertyId}/marketing-audio/` (not gallery `settings.media`). **`?property_id=`**. **`serveAuthenticated`**. GET **`marketing:view`** + `marketingStudio`; POST **`marketing.content:edit`** + `marketingStudio`. |
| `generate-marketing-media` | POST | authenticated JWT + property | Marketing Studio **Generate** tab — a prompt plus the host's own reference photos in, a finished AI image or video out. Body **`{ mediaType: 'image'\|'video', prompt, negativePrompt?, qualityTier?, aspectRatio?, imageSize?, resolution?, durationSeconds?, referenceIds? }`**. Gate order: property scope → **`marketing.generate:add`** (image) or **`marketing.generate.video:add`** (video) + the matching plan feature → rate limit (20/5min image, 5/5min video) → option validation → `assertOrgAndPropertyAiQuota` → `assertMarketingGenerationBudget` (concurrency 2/property, 5/org; in-flight reservation; 60% per-feature sub-cap) → insert `marketing_generation_jobs` row. **Image** runs **inline** (5-15s, same pattern as `generate-marketing-template`) → stores to `property-media` at `marketing-ai/{propertyId}/{jobId}{ext}` → bills via `recordAiUsage` (`cost_basis: 'tokens'`) → logs `marketing.image_generated` → returns the **completed** row. **Video** submits to Veo's `:predictLongRunning` (`startMarketingVideoJob`), stores the operation name, flips to `processing`, logs `marketing.video_generation_started`, and returns immediately — billing and the output happen later, in `get-marketing-generation-job` or the sweeper. Both branches return **`{ job }`** so the client always hands the row to the poller. 429 `upgradeHook` on quota, 429 `retryable` on concurrency/rate limit, 503 on kill switch, 403 if `qualityTier` is `premium` and the property hatch is off, 400 + no charge on a safety block. **`?property_id=`**. **`serveAuthenticated`**. |
| `get-marketing-generation-job` | GET | authenticated JWT + property | Poller for one generation job: **`?jobId=`** → **`{ job }`**. Gated on **`marketing:view`** with **no plan-feature argument** — the view-past-output rule, so a downgraded org still watches an in-flight job finish and reads past results. For a `processing` video job with an operation name and `last_provider_poll_at` older than 10s (or never polled), re-polls Veo inline via `pollAndFinalizeMarketingVideoJob` before responding — on a winning finalize, logs `marketing.video_generated` with a `dashboard` actor. The 10s floor honors Google's recommended minimum poll interval while the client itself polls every 3s. **`?property_id=`**. **`serveAuthenticated`**. |
| `marketing-generations` | GET, DELETE | authenticated JWT + property | **GET** gallery, full keyset paged on `(created_at, id) DESC` — the cursor is `"<created_at>|<id>"`, so two jobs inserted in the same instant cannot skip a row (**`?mediaType=&limit=&cursor=`**, limit capped 50, default 24) → **`{ jobs, nextCursor, allowPremiumImage, allowPremiumVideo }`**; **`marketing:view`**, no plan gate. Premium flags are read-only (no settings row is created). **DELETE** **`{ jobId }`** → removes the storage object and the row, logs `marketing.generated_asset_deleted`; **`marketing.generate:add`**. **`?property_id=`**. **`serveAuthenticated`**. |
| `upload-marketing-generation-reference` | GET, POST, DELETE | authenticated JWT + property | Reusable reference library for the Generate tab. **GET** `{ references }` (`marketing:view`). **POST** multipart **`file`** → magic-byte sniff (`sniffVisualMime`; declared/actual mismatch rejected), `UPLOAD_MAX_BYTES` (10 MB image / 50 MB video), stored at `marketing-ai-refs/{propertyId}/{uuid}{ext}` in **`property-media`**, row in `marketing_generation_references`. **DELETE** **`{ referenceId }`** → storage remove + row delete. Rate limit 30/5min per user. POST/DELETE **`marketing.generate:add`** + `aiMarketingImageGeneration`. **`?property_id=`**. **`serveAuthenticated`**. |
| `upload-marketing-asset` | POST | authenticated JWT + property | Persistent upload for the Design canvas — collage cell photos and anything dropped through Polotno's own Upload panel. Fixes the `blob:`-URL persistence bug (a session-only object URL previously got written into `design_json.polotno` and died on reload). Multipart **`file`** (image only — JPEG/PNG/WebP/HEIC/HEIF, magic-byte sniffed, 10 MB ceiling) → stored at **`marketing-uploads/{propertyId}/{uuid}{ext}`** in **`property-media`** (a dedicated prefix: no gallery mutation, no auto-prune) → **`{ url, storagePath, width, height }`**, no DB row. Rate limit 60/5min per user. **`marketing.templates:add`** + `marketingStudio`. **`?property_id=`**. **`serveAuthenticated`**. Activity log: N/A — staging artifact for a design; the template save that follows is already covered. |
| `marketing-generation-sweeper` | POST (cron) | `X-Marketing-Generation-Cron-Secret` | 5-pass sweep for in-flight video jobs, hosted `pg_cron` every minute: (1) reclaim `finalizing` rows stuck >2min back to `processing`; (2) re-poll + finalize `processing` video jobs due for a check (>10s since last poll), logging `marketing.video_generated` with a `cron` actor on a win; (3) fail in-flight jobs past `expires_at` (`error_code='timeout'`, no charge); (4) bill `completed` rows whose `usage_recorded_at` is still null after 5 minutes (crash-repair); (5) prune up to 50 unused references older than 90 days. Returns `{ reclaimed, finalized, expired, repaired, pruned }`. Env `MARKETING_GENERATION_CRON_SECRET` (+ Vault key `marketing_generation_cron_secret`); `sync_marketing_generation_cron_job()` rebuilds the cron job. **`serveCronPost`**. |
| `list-bookings` _(new)_ | GET | admin JWT | Paginated admin list. **Org:** `?org_slug=` / `?org_id=` → property stays **union** parking reservations; optional `?booking_kind=property\|parking`, `?property_id=`. **Parking:** `?parking_id=`. **Property:** `?property_id=` (stays only). Default `sort=status_priority:asc`; search/status/date/pet/parking filters. With **`expand_imported_batch=true`** and **`status=IMPORTED`**, also returns batch-linked rows (future imports at `PENDING_REVIEW`). Rows include `booking_kind`, `property_*`, `parking_*` meta when applicable. **Pagination:** a single-property/single-parking scope with `sort=check_in_date:*\|created_at:*` and no `from`/`to` filter runs one SQL query (`.order()`+`.range()`+exact count) via the generated `check_in_date_sql`/`check_out_date_sql` columns on `guest_submissions`; every other combination (the `status_priority:asc` default, org-wide scope, or a date-range filter) still fetches all matching rows and sorts/paginates in memory — see `_shared/databaseService.ts#listBookings` and [[../workflow/planned/production-readiness-checklist/10-paginate-large-lists.md]]. |
| `import-parse-file` | POST | admin JWT + `bookings.import:add` | Multipart CSV/Excel upload → private **`import-uploads`** bucket; PapaParse (CSV) or SheetJS (`.xlsx`/`.xls`, first sheet); creates `import_batches` + `import_batch_rows`. Caps: **2,000 rows**, **15 MB**. **`?property_id=`** required. **`serveAuthenticated`**. |
| `import-ai-map-columns` | POST | admin JWT + `bookings.import:add` | `{ batchId }` — Gemini/Groq column-mapping suggestions → `import_batches.column_mapping`; batch `uploaded` → `mapped`. Graceful no-AI fallback (all columns `unmatched`). **`?property_id=`**. |
| `import-save-mapping` | POST | admin JWT + `bookings.import:add` | `{ batchId, columnMapping }` — persist user-confirmed header → field map. **`?property_id=`**. |
| `import-preview` | POST, GET | admin JWT + `bookings.import:add` | Apply mapping, validate rows, batch → `previewed`. Returns row-level errors/warnings. **`?property_id=`** (+ optional `?batchId=` on GET). |
| `import-update-row` | POST | admin JWT + `bookings.import:add` | Toggle skip/include: `{ batchId, rowId, validationStatus: 'skipped' \| 'valid' }`. Skip keeps existing `validation_errors` so the Fix modal stays useful; Restore (`valid`) re-validates and may return `error`. Fix flagged fields: `{ batchId, rowId, fieldValues: Record<string,string> }` — patches mapped CSV cells, re-validates row, returns `mappedData` + refreshed `summary`. Mutually exclusive body shapes. **`?property_id=`**. |
| `import-commit` | POST | admin JWT + `bookings.import:add` | `{ batchId }` — direct insert into `guest_submissions` with **`imported_from_batch_id`**; **past** check-in → **`status='IMPORTED'`**, **today/future** → **`PENDING_REVIEW`** (Asia/Manila); **bypasses** `workflowOrchestrator` (no email/PDF). Partial per-row success. **`?property_id=`**. |
| `import-revert` | POST | admin JWT + `bookings.import:add` | `{ batchId, dryRun?, includeMoved? }` — cancel committed rows via `IMPORTED → CANCELLED` manual override (`ctx.manual=true`, side effects off). Batch → `reverted`. **`?property_id=`**. |
| `import-cancel` | POST | admin JWT + `bookings.import:add` | `{ batchId }` — delete pre-commit batch (rows + storage). Blocked once committed/reverting. **`?property_id=`**. |
| `import-list-batches` | GET | admin JWT + `bookings.import:add` | Paginated import history for property. **`?property_id=`**, optional `limit`/`page`. |
| `create-parking-booking` | POST | admin JWT + parking `bookings:edit` | `{ parkingId, primaryGuestName, guestEmail, guestPhoneNumber, checkInDate, checkOutDate, carPlateNumber, … }` → `guest_submissions` row with `parking_id` (no stay orchestrator). `?parking_id=` |
| `transition-parking-booking` | POST | admin JWT + parking `bookings:edit` | `{ bookingId, toStatus }` — parking-only status path (`PENDING_REVIEW` → `READY_FOR_CHECKIN` → `COMPLETED` \| `CANCELLED`); no email side effects. Rejects `fromStatus = PENDING_HOST_ACCEPTANCE` — that state only resolves via claim/decline/expire below. `?parking_id=` |
| `submit-parking-booking-request` | POST | anon (public) | Guest parking broadcast submit — `{ parkingId?, organizationId?, checkInDate, checkOutDate, vehicleType, primaryGuestName, guestEmail, guestPhone }`. Resolves org-scoped candidates (`accepted_vehicle_types`, `status='ACTIVE'`, no date overlap); **422 `no_parking_available`** with no insert if zero candidates. Inserts `guest_submissions` at `PENDING_HOST_ACCEPTANCE` (`parking_id = null`) + fans out Telegram/email to every candidate host; rolls back the booking on fan-out failure. See `[[parking-workflow]]`. |
| `claim-parking-booking` | POST | authenticated JWT + parking `bookings:edit` | `{ bookingId, parkingId, endorsementNote? }` — atomic first-Accept-wins via guarded `UPDATE ... WHERE status='PENDING_HOST_ACCEPTANCE'`; **409 `already_claimed`** for every other racer. Sends guest confirmation email (+ optional endorsement note, ≤2000 chars server-side). |
| `decline-parking-booking` | POST | authenticated JWT + parking `bookings:edit` | `{ bookingId, parkingId }` — marks this host's broadcast candidacy declined; if it was the last pending candidate, terminates the booking to `NO_HOST_AVAILABLE` immediately (guest emailed once) instead of waiting for TTL. |
| `expire-parking-broadcasts` | POST | cron (`verify_jwt=false`) | Global sweep: `PENDING_HOST_ACCEPTANCE` rows past `parking_broadcast_expires_at` (TTL 15 min same-day check-in / 1 hr advance, Asia/Manila) → `NO_HOST_AVAILABLE`, guest emailed once. Idempotent guarded-UPDATE. Scheduled via `public.sync_parking_broadcast_expire_cron_job()` (migration `20261018120000_parking_broadcast_expire_cron.sql`, every 5 min) — see `docs/archive/operations/scheduled-jobs-and-testing.md`. Optional `PARKING_BROADCAST_EXPIRE_CRON_SECRET` / header `X-Parking-Broadcast-Expire-Cron-Secret`. |
| `send-parking-reminders` (Phase 7) | POST | cron (`verify_jwt=false`) | Daily sweep: property bookings with `need_parking = true`, linkable status, no linked marketplace booking yet, check-in within 3 days, `parking_reminder_sent_at IS NULL` → one reminder email with the `/parkings` self-serve link, dedupe via guarded-UPDATE on `parking_reminder_sent_at`. Scheduled via `public.sync_parking_reminder_cron_job()` (migration `20261201120200_parking_reminder_cron.sql`, daily 06:00 UTC). Optional `PARKING_REMINDER_CRON_SECRET` / header `X-Parking-Reminder-Cron-Secret`. |
| `calendar-sync-cron` | POST | cron (`verify_jwt=false`) **or** admin JWT + property (scoped "Sync now") | **Global sweep** (no path segment): `CALENDAR_SYNC_CRON_SECRET` / header `X-Calendar-Sync-Cron-Secret` gate, `loadDueCalendarFeeds(100)` ordered by `last_attempted_at` (nulls first), ~55 s time budget, skips any property whose org lacks the `calendarSync` entitlement. Per feed: `runFeedSync` (fetch `.ics` → parse → `diffFeed` → apply `property_blocked_dates` `source='ical_import'` create/reschedule/touch/remove → conflict pass → persist feed poll-state + **`calendar_sync_events`**); `create_bookings` reservation promotion is Phase 2. Emits `calendar_sync_failing` at `consecutive_failures = 4`, `calendar_conflict` on an overlap with a live booking / manual block. **Scoped path** `/{feedId}` or `/{propertyId}` = host "Sync now": `resolveScopedPropertyAccess('pricing.channels:edit')` + `requirePropertyFeature('calendarSync')`, `runFeedSync(feed, { force: true })`. Scheduled via `public.sync_calendar_sync_cron_job()` (migration `20261213120500_calendar_sync_cron.sql`, job `calendar-sync-every-30m`, `*/30 * * * *`). See **[[scheduled-jobs-and-testing]]**. |
| `get-parking-booking-status` | GET | anon (public) | `?bookingId=` — narrow guest-safe projection (status, dates, expiry, slot label, endorsement note, plus guest-shell branding: `parkingName`, `coverImage`, `brandColor`, `logoUrl`, `residenceName`) for `ParkingRequestStatusPage`; 404s identically for missing/non-parking rows (no enumeration surface). Guest UI polls every 4s (documented Realtime deviation, see `[[parking-workflow]]`). |
| `get-parking-broadcast-status` | GET | authenticated JWT + parking `bookings:view` | `?bookingId=&parkingId=` — this host's own broadcast row (`pending`\|`claimed`\|`declined`\|`expired`) for `ParkingBookingDetailPage` Accept/Decline visibility. |
| `transition-booking` _(new)_ | POST | admin JWT | `{ bookingId, toStatus, payload, devControls, manual }` → validates via status machine → `WorkflowOrchestrator`. Concurrent Proceed against a stale `from` status returns **409**. |
| `upload-booking-asset` _(new)_ | POST | admin JWT | Multipart upload; `assetType` → Storage bucket + DB column (includes `guest_balance_payment_receipt`, `parking_payment_receipt`, `sd_refund_receipt`); **payment receipt types** run Gemini Flash AI validation when `GEMINI_API_KEY` is set (`payment_receipt`, `guest_balance_payment_receipt`, `parking_payment_receipt`, `sd_refund_receipt`), extracting a peso amount + transaction date and appending an amount-below-minimum or unreasonable-date `⚠` warning onto the persisted summary (`applyReceiptSanityChecks`) — advisory only, never changes the verdict or blocks a transition; balance upload fires instant admin Telegram notify and patches AI Summary **Pricing** flags from the stored verdict (no extra Gemini); guest doc types `payment_receipt` / `valid_id` / `pet_vaccination` / `pet_image` also revert revert-eligible statuses → `PENDING_REVIEW` (see §5.2) |
| `parking-broadcast-email` | POST | admin JWT | **Retired (410)** — legacy property parking broadcast removed. Use **Find parking** / marketplace `linkStay` from booking detail. |
| `send-booking-workflow-email` | POST | admin JWT | **`{ bookingId, kind }`** — manual (re-)send of a workflow email. `kind`: `gaf_request` \| `pet_request` \| `booking_acknowledgement` \| `ready_for_checkin` \| `sd_refund_form_request`. Uses property Email Automations toggles only (**not** plan-gated) — Free-tier escape hatch when `automatedBookingFlow` skips auto-send. Stamps `workflow_email_manual_sent_at[kind]`. On Free, same-kind resend within **1 hour** returns **429**. Does not change status (SD path updates `sd_refund_form_emailed_at`). |
| `validate-booking-receipts` _(new)_ | POST | admin JWT | `{ bookingId }` — one-shot AI backfill for document URLs missing AI verdict columns (downpayment, balance, parking, SD refund receipts; guest valid ID), same amount/date sanity warnings as `upload-booking-asset`; skips `COMPLETED` / `CANCELLED`. Not called from booking-detail UI (page-open auto-backfill was removed — `useReceiptAiBackfill` is a no-op helper only). See **[[ai-payment-receipt-validation]]** §4.4. |
| `booking-ai-review` _(new)_ | POST | admin JWT | `{ bookingId, force?, refresh? }` — admin-triggered, synchronous, multi-section AI summary + validation job. Runs Stay Details (SQL), Guests (batched Gemini vision on all present ID images), Pricing (Gemini downpayment receipt extraction + stored guest-balance receipt flags), Parking (derived from Pricing), and Pets (if `has_pets`) with ≤3 Gemini calls total. A `completed` job is returned as-is unless `refresh: true` and at least one section fingerprint no longer matches the live booking (unchanged sections skip Gemini). Upserts `booking_ai_reviews` section-by-section and persists per-slot ID verdicts/summaries to `guest_submissions`. Response includes computed `stale_sections`. `resolveScopedPropertyAccess(req, 'bookings.detail.stay:edit')`. See **[[guides/routes/org/property/bookings-detail]]** §AI Summary & Validation. |
| `get-booking-ai-review` _(new)_ | GET | admin JWT | `?bookingId=` — returns the current `booking_ai_reviews` row for polling; UI refetches while `job_status === 'processing'`. Completed/failed rows include computed `stale_sections` (section ids whose inputs drifted — not a DB column). `resolveScopedPropertyAccess(req, 'bookings.detail.stay:edit')`. |
| `get-booking-asset-url` _(new)_ | POST | admin JWT | Resolves browser-viewable links for storage assets; public buckets return normalized URL, private buckets return short-lived signed URL |
| `approval-email-webhook` | POST | Resend Svix | `email.received` → plus-address property routing → Storage upload → `WorkflowOrchestrator`. Ops: **[[approval-email-inbound]]**. |
| `sd-refund-cron` _(Phase 4)_ | POST | cron/internal **or** admin JWT | **Global:** empty/`{}` body — every `READY_FOR_CHECKIN` row inside the pre-check-out **lead** window (Manila, `SD_REFUND_CRON_EMAIL_LEAD_MINUTES`, default 120): sends **Check-out & SD Refund** guest email when not already sent (not gated on balance settlement). **Transition** to `READY_FOR_CHECKOUT` only when guest balance settlement is complete (same rules as admin). **Scoped:** JSON `{ "bookingId" }` + admin JWT — one id. **Stale check-outs:** automated guest email suppressed when check-out is older than `SD_REFUND_CRON_MAX_CHECKOUT_AGE_DAYS` (default 30; `0` = never suppress); transition still runs when settlement is met |
| `telegram-marketing-cron` | POST | cron/internal | **Daily** at times stored as **`telegram_marketing_settings.daily_reminder_times_manila`**: Postgres RPC **`sync_telegram_marketing_daily_cron_jobs`** creates one **`telegram-marketing-daily-slot-*`** `pg_cron` job per unique slot (converted Manila +08 → UTC `minute hour * * *`; legacy **`telegram-marketing-daily-manila`** is dropped on reschedule). Sends default or urgency Telegram copy + calendar availability. Optional header **`X-Telegram-Cron-Secret`** when **`TELEGRAM_CRON_SECRET`** is set. **`verify_jwt = false`** — see **[[telegram-marketing-reminders]]** |
| `telegram-marketing-settings` | GET, PATCH, POST | admin JWT | **GET** templates + toggles + **`dailyReminderTimesManila`** + UTC cron preview strings. **PATCH** camelCase (`dailyReminderTimesManila` optional array of **`{hour,minute}`** Manila → persists row + invokes **`sync_telegram_marketing_daily_cron_jobs`**; response may include **`cronSync`**). **POST** `action`: `verify_telegram_env` \| **`send_draft_preview`** \| **`render_draft_preview`** + **`text`** (optional **`checkInYmd` / `checkOutYmd`** for `{{cancellation_dates}}`; live calendar placeholders; **400** if missing data; **`render_draft_preview`** returns **`{ renderedText, placeholders? }`** without sending). **`verify_jwt = false`**; **`verifyAdminJwt`** |
| `telegram-staff-cron` | POST | cron/internal | **Daily** at time stored as **`telegram_staff_settings.daily_summary_time_manila`** (default 8:00 AM Manila): sends **active** today's booking summary (new check-ins and in-house multi-night stays — **not** checkout-only days) + next 3 days' **upcoming check-ins** to staff Telegram group. If no active bookings today, sends **`daily_summary_no_bookings_template`** (default general cleaning checklist + `{{next_bookings}}`). Uses `TELEGRAM_STAFF_BOT_TOKEN` (fallback `TELEGRAM_BOT_TOKEN`) + `TELEGRAM_STAFF_CHAT_ID`. Optional `X-Telegram-Cron-Secret` via `TELEGRAM_STAFF_CRON_SECRET` |
| `telegram-staff-settings` | GET, PATCH, POST | admin JWT | **GET** templates + toggles + time. **PATCH** camelCase (`dailySummaryTemplate`, `dailySummaryNoBookingsTemplate`, `sameDayCheckinTemplate`; `dailySummaryTimeManila` `{hour,minute}` Manila → persists + invokes **`ensure_telegram_multi_property_cron_dispatch`** when time changes; response may include **`cronSync`**). **POST** `action`: `verify_staff_telegram_env` \| `send_draft_preview` \| **`render_draft_preview`** + `text` + `scenario` (`daily_summary` \| `daily_summary_no_bookings` \| `same_day_checkin`). **`verify_jwt = false`**; **`verifyAdminJwt`** |
| `telegram-finance-settings` | GET, PATCH, POST | admin JWT | **GET/PATCH** global finance reminder config (`enabled`, `defaultReminderTemplate`, `dailyCheckTimeManila` → `sync_telegram_finance_daily_cron_job`). **POST** `action`: `verify_finance_telegram_env` \| `send_test_due_reminders` \| `send_draft_preview` \| **`render_draft_preview`** + `text`. **`verify_jwt = false`**; **`verifyAdminJwt`** |
| `telegram-chat-settings` | GET, PATCH, POST | admin JWT | **GET/PATCH** per-property or per-parking guest chat Telegram alerts (`enabled`, `newMessageTemplate`, credentials). Scope via `property_id` or `parking_id`. Fires on inbound **web** + Meta DM guest messages when enabled. Placeholders: `guest_name`, `property_name` (property or parking slot name), **`chat_source`**, `chat_content`, `attachment_summary`, `attachment_line`, `conversation_link`, inquiry dates, `sent_at`. **POST** `action`: `verify_chat_telegram_env` \| `send_draft_preview` \| **`render_draft_preview`**. **`verify_jwt = false`**; **`verifyAdminJwt`** + property/parking scope. |
| `telegram-global-settings` | GET, PATCH, POST | admin JWT | **GET/PATCH** shared Telegram bot token for Notifications modules (`telegram_global_bot_token_encrypted` on **`app_settings`** or **`parking_settings`**). Pre-fills module bot token fields when unset. **POST** `action`: **`verify_global_telegram_bot`** (+ optional draft **`botToken`**) — Telegram **`getMe`** only. **`?property_id=`** or **`?parking_id=`**. **`verify_jwt = false`**; **`verifyAdminJwt`** + **`notifications:edit`** (property) or parking manage. |
| `telegram-parking-settings` | GET, PATCH, POST | admin JWT | **GET/PATCH** per-parking Telegram bot + three templates (`reservationRequestTemplate`, `checkInReminderTemplate`, `paymentReceivedTemplate`) + notify toggles. **`?parking_id=`** required. **POST** `action`: `verify_parking_telegram_env` \| `send_draft_preview` \| **`render_draft_preview`** + `text`. **`verify_jwt = false`**; **`verifyAdminJwt`** + parking access |
| `telegram-finance-cron` | POST | cron/internal | **Hourly** (`0 * * * *`): scans `finance_line_items` with `telegram_reminder_enabled`, no `paid_at`, inside each row's days-before window through due date. Cadence per row: `hourly`, `every_2_hours`, `every_4_hours`, `every_12_hours`, `daily_noon` (12:00 PM Manila). At most **one** reminder per **`recurrence_series_id`** per run (earliest in-window occurrence). Mark as paid stops reminders for all intervals. Dedupes via last `finance_telegram_reminder_log.sent_at`. Uses `TELEGRAM_FINANCE_BOT_TOKEN` (fallback `TELEGRAM_BOT_TOKEN`) + `TELEGRAM_FINANCE_CHAT_ID`. Optional `X-Telegram-Cron-Secret` via `TELEGRAM_FINANCE_CRON_SECRET` |
| `telegram-admin-cron` | POST | cron/internal | **Hourly** (`0 * * * *` UTC): scans bookings for **Pending Review** new-booking reminders, pending docs on check-in day, missing balance receipt **during the stay** (after check-in time through check-out), and SD refund awaiting admin processing. Dedupes via **`telegram_admin_notification_log`** (send-then-record; includes **`new_booking`** type per migration **`20260705120000`**). Response includes **`matchedNewBooking`**, **`matchedPendingDocs`**, etc., and **`detail`** when **`mode`** is `nothing_due`. Uses `TELEGRAM_ADMIN_BOT_TOKEN` (fallback `TELEGRAM_BOT_TOKEN`) + `TELEGRAM_ADMIN_CHAT_ID`. Optional `X-Telegram-Cron-Secret` via `TELEGRAM_ADMIN_CRON_SECRET` |
| `telegram-admin-settings` | GET, PATCH, POST | admin JWT | **GET** six scenario templates + toggles + placeholder reference. **PATCH** camelCase fields → persists; optional **`resyncHourlyCron`** invokes **`sync_telegram_admin_hourly_cron_job`**. **POST** `action`: `verify_admin_telegram_env` \| `send_draft_preview` \| **`render_draft_preview`** + `text` + `scenario`. **`verify_jwt = false`**; **`verifyAdminJwt`** |
| `app-settings` | GET, PATCH, POST | admin JWT | **GET** resolved **property** operator config from **`app_settings`** (payment, GAF fields, **email routing**, SD cron tuning, parking default, **`automationToggles`**, **`documentRequirementsOverride`**, **`vouchersEnabled`**, **`voucherPrizes`**, **`voucherRevealStyle`** (`reel`\|`wheel`\|`flip`)) merged with org **branding/social/logo** from **`org_settings`** + **`propertyIntegrations`** + **`platformSecrets`**. **PATCH** property-scoped camelCase fields (payment, GAF, email recipients/timing, toggles, document override, voucher prizes/style); image resets via empty string for **`gcashQrImageUrl`** / **`gafUnitOwnerSignatureUrl`**. **Payment PATCH** requires **`settingsVerificationToken`** from **`settings-verification`** when **`paymentMethods`** (or other payment keys) change. **POST** `action`: **`verify_ai`** (alias **`verify_gemini`**). **`?property_id=`** required (or first owned property). **`verify_jwt = false`**; **`verifyAdminJwt`**. Resolution helpers: **`documentRequirements.ts`**. |
| `settings-verification` | POST | authenticated JWT + property/parking scope | **`send_otp`** — emails 6-digit code to **org owner** only (branded shell; names actor + role); body **`{ action, patchFingerprint }`**, query **`?property_id=`** or **`?parking_id=`**. **`verify_otp`** — body **`{ challengeId, code }`** → short-lived **`verificationToken`** for gated PATCH. Rate limit 3 sends / 15 min per user+org. |
| `super-admin-verification` | POST | super-admin | Step-up OTP for sensitive `/admin/*` mutations. **`send_otp`** — 6-digit code (hashed, 10-min TTL, 3 / 15 min per user) to the **acting super admin's own login email**; optional body **`{ gatedAction }`** for email context. **`verify_otp`** — body **`{ challengeId, code }`** → HMAC **`verificationToken`** (15-min sudo window, bound to `user.id`) the client replays as the **`x-superadmin-otp`** header. Challenges in **`super_admin_verification_challenges`**. Enforced by **`requireSuperAdminStepUp`** in each gated `serveSuperAdmin` fn (`org-subscriptions-admin`, `platform-payment-settings`, `platform-parking-settings`, `parking-payouts`, `ai-platform-credit-wallet`, `ai-platform-global-settings`, `dashboard-assistant-global-settings`, `pricing-plans`, `platform-settings`, `update-platform-host-settings`, `decide-contract-consideration`). **`verify_jwt = false`**. |
| `property-pricing` | GET, PATCH | admin JWT | **GET** property nightly rate defaults (`weekdayNightlyRate`, `weekendNightlyRate`), fee defaults (`downPayment`, `securityDeposit`, `petFee`, `parkingRateGuest`, `guestAdditionalFee`), `dateOverrides` map, `bookedDateKeys` (optional `?month=YYYY-MM` scopes booked nights), `blockedDateKeys` (owner date blocks, same month scoping, from **`property_blocked_dates`**), and `importedBlockedDateKeys` (the `source='ical_import'` subset — OTA-synced nights, read-only in the calendar). **PATCH** saves defaults + full replace of date overrides, plus `blockRange: { startDate, endDate, note? }` (creates a block) and `unblockDateKeys: string[]` (frees nights, splitting partially-covered ranges). Permission **`pricing:view`** / **`pricing:edit`**. **`?property_id=`**. **`verify_jwt = false`**; **`verifyAdminJwt`** + property team permission. |
| `calendar-sync-settings` | GET, PATCH | admin JWT + property | **Airbnb / OTA calendar sync** (`calendarSync`, Pro+). **GET** (`pricing.channels:view`, **no plan gate** — preview-open): connected feeds (masked `.ics` URL + `health` derived from `consecutive_failures` — `ok` / `warning` 1-3 / `error` ≥4), export URLs (`all` + per-provider `?as=`), 30 most-recent **`calendar_sync_events`**. **PATCH** (`pricing.channels:edit` + `requirePropertyFeature('calendarSync')`) `action`: `addFeed` (SSRF-shape check + `encryptIntegrationSecret`), `updateFeed` (URL change resets health), `removeFeed` (`deleteData=false` → demote synced rows to `source='manual'`; `true` → CASCADE delete), `setExportEnabled`, `syncNow` (`runFeedSync(feed, { force: true })`). **`?property_id=`**. **`verify_jwt = false`**; **`serveAuthenticated`** + property team permission. Guide: **[[guides/routes/org/property/pricing]]**. |
| `property-templates-settings` | GET, PATCH | admin JWT | **GET** built-in + custom template content from **`property_template_contents`** merged with shipped defaults (`propertyTemplates.ts`). **PATCH** `{ templateKey, content, sectionImageUrl? }` save; `{ action: "create", name, content }` custom template; `{ action: "delete", templateKey }` custom only. **`?property_id=`**. **`verify_jwt = false`**; **`verifyAdminJwt`**. Live workflow email bodies for the seven **email** keys are read at send time via **`propertyTemplateEmail.ts#renderPropertyTemplateSendEmail`**. |
| `custom-pages-settings` | GET, PATCH | admin JWT | **GET** property **`custom_pages`** rows (`stay_guide` / `property_landing` / `property_showcase`); **PATCH** stay_guide + showcase `template_key` → **`publicPagesAutosave`** (Pro+). Lazy-create on read. **`?property_id=`**. **`verify_jwt = false`**; **`verifyAdminJwt`** + public-pages permissions. Guide: **[[guides/routes/org/property/public-pages]]**. |
| `public-page-configs` | GET, PATCH | admin JWT | **GET/PATCH** section visibility/order/style in **`public_page_configs`** (`page_type`: `stay_guide` \| `property_landing` \| `property_showcase`). Lazy-create on GET. PATCH gated by **`publicPagesAutosave`**; showcase publish/template additionally by **`propertyShowcase`**. Permissions per leaf (`publicPages.property:edit` / `stayGuide:edit` / `showcase:edit`). **`?property_id=`** + **`page_type=`** (GET) / body `{ pageType, config }` (PATCH). |
| `upload-property-template-asset` | POST | admin JWT | Multipart: **`section_image`** (standard template key → persists **`section_image_url`**) or **`inline_image`** (WYSIWYG embed URL only). **`?property_id=`**. |
| `property-templates-preview` | POST | admin JWT | Body `{ templateKey, category, content, name? }` → `{ html, mode }` email-shell or standard preview HTML with sample placeholders + property branding. **`?property_id=`**. **`static_files`** for email template fragments. Does not affect live sends. |
| `org-settings` | GET, PATCH | admin JWT | **GET/PATCH** organization-scoped operator config in **`org_settings`**: **social links** (`facebookPageUrl` → `facebook_reviews_url`, `instagramUrl`, `tiktokUrl`), team logo reset. Email automations are **per property** (`app_settings`). **Not** editable: guest app origin (env **`PUBLIC_GUEST_APP_ORIGIN`**). Query **`?org_id=`** or **`?org_slug=`**. |
| `upload-app-settings-asset` | POST | admin JWT | Multipart upload for **property** assets. **`gcash_qr`**: stages unique Storage path, returns URL only — does **not** write `app_settings` (commits via OTP-gated **`app-settings` PATCH** `paymentMethods`). **`gaf_unit_owner_signature`** → **`gaf_unit_owner_signature_url`** immediately. External review image / stay-photo assets as documented. JPEG/PNG/WebP, max 5 MB (signature: PNG/JPEG only). **`?property_id=`**. **`verify_jwt = false`**; **`verifyAdminJwt`**. |
| `upload-property-media` | POST, DELETE | admin JWT | **POST** multipart gallery upload → public URL in Storage **`property-media`** (`{propertyId}/{mediaId}.{ext}`) and append to **`properties.settings.media`**. **DELETE** JSON `{ mediaId, storagePath? }` removes DB row + storage object. Supported image/video MIME types; max **9** images (5 MB each) and **1** video (25 MB). Reorder/primary via **`update-property`** **`settings.media`**. **`?property_id=`**. **`verify_jwt = false`**; **`verifyAdminJwt`**. |
| `upload-org-settings-asset` | POST | admin JWT | Multipart upload for **org** team logo (**`team_logo`** → **`org_settings.email_logo_url`**, path `team-logo/org/{orgId}/…`). JPEG/PNG/WebP, max 5 MB. Query **`?org_id=`** or **`?org_slug=`**. **`verify_jwt = false`**; **`verifyAdminJwt`** + org ownership |
| `get-sd-form` | GET | anon (public) | Query `?bookingId=` — minimal payload for `/sd-form` when status is `READY_FOR_CHECKOUT`, **or** `READY_FOR_CHECKIN` with `sd_refund_form_emailed_at` set (early cron email). Returns `awaiting_balance_settlement: true` until status advances. Includes `primary_guest_name`, check-in/out dates, voucher fields for replay (`vouchers_enabled`, `voucher_prizes`, `voucher_reveal_style`: `reel` \| `wheel` \| `flip`). After reveal, **`VoucherReveal`** shows guest + stay on the voucher card. |
| `get-guest-review` | GET | anon (public) | Query `?bookingId=` — Airbnb + `security_deposit = 0` post-stay review bootstrap for **`/properties/:slug/guest-review`**. Available from check-out date (Manila) through **`COMPLETED`**. Same review + voucher UI as SD step 1 (no refund step); includes `voucher_reveal_style`. |
| `get-guest-stay-guide` | GET | anon (public) | Query `?token=` (required), optional `?property=<slug>`. Token-gated brochure payload for **`/properties/:slug/stay-guide`** — standard property templates + media + contact. Valid only during booking access window on `guest_submissions` (`stay_guide_*` columns). Guide: **[[stay-guide]]**. |
| `preview-guest-stay-guide` | GET | authenticated JWT + property RBAC | Query `?property_id=` (required), optional `?property=<slug>`. Admin stay-guide preview with **mock booking placeholders** for **`/properties/:slug/stay-guide?preview=1`**. **`templates:view`**. Guide: **[[stay-guide]]**. |
| `issue-guest-stay-guide-token` | POST | authenticated JWT + property RBAC | Body **`{ bookingId }`**. Issues or refreshes stay-guide token; returns **`stayGuideUrl`**. RFCI+ only. **`bookings:workflow`**. |
| `issue-guest-form-completion-token` | POST | authenticated JWT + property RBAC | **Calendar sync Phase 2.** Body **`{ bookingId }`**. Mints/rotates `guest_form_token`; returns **`completionUrl`** (`<origin>/form?complete=<token>&property=<slug>`). Only for OTA-sourced bookings (`external_source` set or `booking_source='Airbnb'`) still `PENDING_REVIEW` with a future check-out (409 / 410 otherwise). **`bookings.detail.workflow:edit`**. |
| `claim-sd-voucher` | POST | anon (public) | Body `{ bookingId }`. Requires an existing guest review. Idempotently rolls from property `app_settings.voucher_prizes` (`[{code, percentOff, chancePercent}]` where `chancePercent` is a **relative weight**; empty/legacy → platform % defaults in `_shared/voucher.ts`) and persists `next_stay_voucher_code` / `_amount` (= percent off) / `*awarded_at`. Conditional write when code is null (race-safe). Stamps `guest_user_id` when a guest JWT is present. Re-calls return the existing voucher with `alreadyAwarded: true`. Rejects new rolls when `vouchers_enabled` is false. Status guard: **`READY_FOR_CHECKOUT`** (SD path), or **`READY_FOR_CHECKOUT`/`COMPLETED`** for Airbnb zero-SD bookings. |
| `submit-sd-form` | POST | anon (public) | Guest submits refund preference (`refund`body); optional`guestFeedback`(stored when sent). →`PENDING_SD_REFUND`via orchestrator. For`refund.method = other_bank`, **`refund.bank`** must be **`GCash`**, **`GoTyme`**, or **`Maribank`** (see migration **`20260607130000_sd_refund_bank_gotyme.sql`**). |
| `get-pay-parking` | GET | anon (public) | Query`?bookingId=` — booking summary + **`parking_check_in_date`**, **`parking_check_out_date`**, **`number_of_parking_nights`** (falls back to stay dates when unset) + existing vehicle fields for **`/bookings/:bookingId/parking`**. **404** when missing id or **`CANCELLED`**. Default **`parking_rate_guest`** from **`app_settings`** (fallback env/default ₱400) when unset on the booking. |
| `submit-pay-parking` | POST | anon (public) | Body `{ bookingId, carPlateNumber, carBrandModel, carColor }`. Saves vehicle fields and sets **`need_parking`**; uses **`parking_rate_guest`** from DB only. **Does not** send parking owner emails (retired — use marketplace). Clears **`parking_completed_at`** when the booking is **`READY_FOR_CHECKIN`+** or parking was previously marked complete. Does **not** change workflow **`status`**. Response **`data.broadcastSent`** is always **`false`**. Legacy guest URLs redirect to marketplace. |
| `send-sd-refund-form-email` | POST | admin JWT | Re-send check-out / SD details email; status must be`READY_FOR_CHECKIN`or`READY_FOR_CHECKOUT`; updates `sd_refund_form_emailed_at` |
|`dashboard-stats`_(new)_ | GET | admin JWT | Admin dashboard aggregates. **Property scope:**`?property_id=…`. **Parking scope:** **`?parking_id=…`** (single slot; `bookings:view`on parking team). **Org scope:**`?org_slug=…`or`?org_id=…` (aggregates org properties + parking listings). Query **`from`** / **`to`** (YYYY-MM-DD) filters period KPIs, trend series, recent bookings, property performance, and **`statusBreakdown`**. Org response adds `trendSeries`, `recentBookings`, `propertyPerformance`, `parkingPerformance`, `statusBreakdown`, `propertyCount`, and `parkingCount`. Recent bookings include `bookingKind` and parking fields. Period KPIs use **operating host net** for **net profit**; **avg nightly rate** = booking rate per occupied night in period; property dashboard **total bookings** card still uses occupied nights / period days; org dashboard **Total Bookings** stat uses check-in count in range (`kpis.checkInsInPeriod`). |
| `finance-summary`_(new)_ | GET | admin JWT | KPI aggregates for finance dashboard:`?basis=check_in\|check_out\|completed`, `?from`/`?to`, `include_cancelled`, `completed_only`, optional `q` |
| `analytics-summary` _(new)_ | GET | admin JWT | Host Analytics deterministic bundle. **Property scope:** `?property_id=…`. `?from`/`?to` (YYYY-MM-DD, defaults to current calendar month, Manila). Computed on-read from `guest_submissions` + `guest_reviews` + `inbox_thread_metrics` + `property_page_views` via `_shared/analyticsService.ts` — no rollup table. Returns `{ tier: 'full', ...AnalyticsBundle }` when the property has the `analyticsInsights` plan feature, else a trimmed `{ tier: 'teaser', kpis: {occupancyRate, adr, revpar, reservations} }` — the deterministic bundle is computed either way, only the response is trimmed. `AnalyticsBundle` includes KPIs (prior + YoY deltas), trend series, distributions (LOS, lead time, channel mix, guest age, guest origins), forward 90-day view, booking pace, pickup, the two-axis state assessment (`forwardOccupancyState30d/60d`, `balanceCollectionState`), and public-page performance (page views/unique visitors/top referrers). Full-tier response also carries `playbook: []` — up to 6 `host_playbook_articles` matched against the bundle via `_shared/hostPlaybook.ts` (fails soft to `[]`). |
| `analytics-ai-review` _(new)_ | GET, POST | admin JWT | GET returns the latest `property_analytics_reviews` row (or `null`). POST regenerates on demand — `requirePropertyFeature('analyticsInsights')` + `assertOrgAndPropertyAiQuota('host_analytics')`, rate-limited 1/hour/property, recomputes the last-30-day `AnalyticsBundle`, calls `maybeRunAnalyticsAiReview` (Gemini, `_shared/analyticsAiReview.ts`) with recent `activity_log` evidence, writes the new row (`is_latest` flip), logs `analytics.review_regenerated`. Returns `{ review: null, available: false }` on any AI failure (no keys/quota/bad JSON) rather than erroring — client shows the last review unchanged. |
| `analytics-org-summary` _(new)_ | GET | admin JWT | Host Analytics Phase 5 org portfolio rollup. **Org scope:** `?org_slug=…`/`?org_id=…`, `?from`/`?to`. Gated `org.analytics:view`. Lists the org's `ACTIVE` properties (cap 200); for each, checks that property's own `analyticsInsights` entitlement — entitled properties get a `_shared/analyticsService.ts#computePropertyPortfolioRow` row (occupancy/ADR/RevPAR/revenue/reservations/cancellation rate/forward state — a lighter query than the full bundle, no reviews/inbox/page-views), non-entitled properties come back `{ locked: true }` with no numbers. Returns `{ period, portfolio: {totalRevenue, avgOccupancy, totalReservations, propertyCount, entitledPropertyCount}, rows }`. Responds `402` if zero properties in the org are entitled. |
| `analytics-ai-review-cron` _(new)_ | POST | `serveCronPost` (secret) | Weekly batch (oldest-reviewed-first, 55s time budget): regenerates the AI Performance Review for every `analyticsInsights`-entitled ACTIVE property with enough booking history (`sufficiency.enough`), notifies `analytics_review_updated` (deduped per property per day) when the score moves ≥8 points. Schedule: `sync_analytics_ai_review_cron_job()`, Sunday 16:00 UTC = Monday 00:00 Asia/Manila. |
| `property-page-view` _(new)_ | POST | public (unauthenticated) | Host Analytics Phase 2b pageview ingest. Body `{ propertyId, sessionId, referrer?, utmSource?, utmMedium?, utmCampaign? }`. Validates the property is `ACTIVE` (silently no-ops otherwise), `rateLimitGate` 1/30s per property+session, basic UA bot-pattern filter + device-class derivation, inserts into `property_page_views`. Called via `navigator.sendBeacon` (anon key as a query param, since `sendBeacon` can't set headers) with a `fetch(keepalive:true)` fallback from the public property page. |
| `property-page-views-prune-cron` _(new)_ | POST | `serveCronPost` (secret) | Monthly retention prune for `property_page_views` — deletes rows past 180 days in bounded 5,000-row batches (cap 20 batches/run, next month continues). Schedule: `sync_property_page_views_prune_cron_job()`, 1st of the month 17:00 UTC = 2nd 01:00 Asia/Manila. |
| `activity-log-retention-cron` _(new)_ | POST | `serveCronPost` (optional `X-Activity-Log-Retention-Cron-Secret`) | Monthly retention purge for `activity_log` — reads `platform_settings.activity_log_retention_months` (default 24, floor 6), calls `purge_activity_log(months, 200000)` which sets `activity_log.allow_purge` and batch-deletes rows past the window. Schedule: `sync_activity_log_retention_cron_job()`, 1st of the month 18:00 UTC = 2nd 02:00 Asia/Manila. |
| `list-host-playbook-articles-admin` _(new)_ | GET | super-admin | All `host_playbook_articles` rows including inactive. Powers `/admin/playbook`. |
| `create-host-playbook-article` _(new)_ | POST | super-admin | New `host_playbook_articles` row — `slug` (unique, immutable), `category`, `title`, `bodyMd`, `appliesWhen` (jsonb condition matched against the `AnalyticsBundle`), `sortOrder`, `isActive`. |
| `update-host-playbook-article` _(new)_ | POST | super-admin | Partial update; also handles the active-toggle. |
| `delete-host-playbook-article` _(new)_ | POST | super-admin | Removes a `host_playbook_articles` row. |
|`finance-bookings`_(new)_ | GET | admin JWT | Paginated stays ledger with computed`financials`per row; same query params +`page`, `limit`, `sort` (`check_in_date:*`, `host_net:_`) |
| `finance-line-items` _(new)_ | GET, POST, PATCH, DELETE | admin JWT + **`property_id`** or **`parking_id`** | CRUD for **`finance_line_items`** (`resolveFinanceAssetAccess`). GET filtered by `occurred_on` range; optional **`q`**; **`?include_due_in_range=true`** also merges rows whose **`telegram_due_date`** falls in range (optional; not used by dashboard). **`?recurrence_series_id=`** returns all rows in a series (ignores period). POST creates one-off or materialized recurring series (`recurrence_interval`+`recurrence_until`); optional Telegram reminder fields on create. POST **`action: extend_series`** with **`recurrence_series_id`**, **`direction`** (`before`\|`after`), **`extend_until`** adds missing occurrences. PATCH body **`scope`** (`this`\|`this_and_future`\|`all`); reminder fields follow scope. PATCH **`recurrence_interval`** / **`recurrence_until`** on a series row rebuilds the whole series (adds/removes occurrences; always series-wide). DELETE `?scope=`same. |
|`finance-export`_(new)_ | GET | admin JWT | CSV download:`?type=overview\|stays\|operating\|transactions\|combined` + same period filters as summary (`transactions`alias for`operating`); **booking_rate** / **other_fees** metrics; **Definitions** block with formulas; stays export includes **TOTALS** row |
| `maintenance-summary`_(new)_ | GET | admin JWT | KPI aggregates for maintenance dashboard:`?from`/`?to`— total, telegram-enabled, completed, pending, category counts |
|`maintenance-items` _(new)_ | GET, POST, PATCH, DELETE | admin JWT | CRUD for **`maintenance_items`**. Same recurrence + Telegram reminder contract as `finance-line-items`(uses`scheduled_on`, `completed_at`instead of`occurred_on`/`paid_at`; no `kind`/`amount`). GET filtered by `scheduled_on` range; optional **`q`**; **`?include_due_in_range=true`** merges rows whose **`telegram_due_date`** falls in range. **`?recurrence_series_id=`** returns series rows. POST **`action: extend_series`** same as finance. PATCH/DELETE **`scope`** same as finance. PATCH **`recurrence_interval`** / **`recurrence_until`** rebuilds the whole series. |
| `telegram-maintenance-settings` | GET, PATCH, POST | admin JWT | **GET/PATCH** global maintenance reminder config (`enabled`, `defaultReminderTemplate`; PATCH triggers **`sync_telegram_maintenance_hourly_cron_job`**). **POST** `action`: `verify_maintenance_telegram_env`\|`send_test_due_reminders`\|`send_draft_preview` \| **`render_draft_preview`** + `text`. **`verify_jwt = false`**; **`verifyAdminJwt`** |
| `telegram-maintenance-cron` | POST | cron/internal | **Hourly** (`0 * * * *`): scans `maintenance_items`with`telegram_reminder_enabled`, no `completed_at`, inside each row's days-before window through due date. Same interval cadence + series dedupe as finance cron. Uses `TELEGRAM_MAINTENANCE_BOT_TOKEN`(fallback`TELEGRAM_BOT_TOKEN`) + `TELEGRAM_MAINTENANCE_CHAT_ID`. Optional `X-Telegram-Cron-Secret`via`TELEGRAM_MAINTENANCE_CRON_SECRET` |
| **`/sd-form` cash pickup:** No guest-entered note — the stepper shows fixed policy copy (cash balance/SD paid in cash, staff on Azure premises, contact Facebook/Airbnb before leaving). The legacy **`sd_refund_cash_pickup_note`** column was removed in migration **`20260607120000_drop_sd_refund_cash_pickup_note.sql`**. **`sd_refund_bank`** allow-list and UI options are **GCash / GoTyme / Maribank** (legacy **BDO** / **BPI** removed in **`20260607130000_sd_refund_bank_gotyme.sql`**). |

**Reset next-stay voucher (local / QA only):** while `status = 'READY_FOR_CHECKOUT'`, clear the three columns so `/sd-form` and `claim-sd-voucher` behave like a first visit again:

```sql
UPDATE guest_submissions
SET
  next_stay_voucher_code = NULL,
  next_stay_voucher_amount = NULL,
  next_stay_voucher_awarded_at = NULL
WHERE id = '<booking-uuid>';
```

Run in the Supabase Dashboard SQL editor (or `psql`). After the guest submits the SD refund form, status is no longer `READY_FOR_CHECKOUT` and the public SD flow is closed—avoid clearing production voucher data on real completed stays unless ops explicitly requires it.

**Scheduled jobs (deep dive + testing):** see **[[scheduled-jobs-and-testing|Scheduled jobs (cron) and how to test them]]** — how `pg_cron` + `pg_net` invoke these URLs on Supabase Cloud, why `schedule` is not in `config.toml` locally, env vars, curl/admin UI test steps, and idempotency.

`transition-booking`, `transition-parking-booking`, and `social-inbox-send` are wrapped with **`_shared/idempotency.ts#withIdempotency`** — an `Idempotency-Key` header (sent by the PWA offline outbox on replay) makes the handler run at most once (claim-first via **`request_idempotency`**). See **[[pwa]]** §6.

**Auth**: `verify_jwt = false` for `submit-form`, `get-booked-dates`, `get-form`, `get-guest-payment-info`, `get-public-property`, `get-public-showcase`, `get-public-parking`, `search-suggestions`, `search-listings`, `list-public-properties`, `list-public-developments`, `list-public-parkings`, `list-public-place-groups`, `list-public-pricing-plans`, `get-public-host`, `get-sd-form`, `get-guest-review`, `get-guest-stay-guide`, `submit-sd-form`, `submit-guest-review`, `claim-sd-voucher`, `list-guest-vouchers`, `get-pay-parking`, `submit-pay-parking`, `submit-parking-booking-request`, `get-parking-booking-status`, `list-linkable-property-bookings` (guest-authenticated, Phase 7), `get-linked-parking-booking` (admin-authenticated property-scoped read, Phase 7), `resolve-owner-default-parking` (admin property-scoped, owner-owned default slot), `ical-export` (token-gated public calendar feed), `get-form-completion` / `submit-form-completion` (calendar-sync guest-form completion, token-gated). Admin functions (`dashboard-stats`, `list-bookings`, `transition-booking`, `upload-booking-asset`, `validate-booking-receipts`, `booking-ai-review`, `get-booking-ai-review`, `send-sd-refund-form-email`, `cancel-booking`, `app-settings`, `org-settings`, `upload-app-settings-asset`, `upload-org-settings-asset`, `finance-summary`, `finance-bookings`, `finance-line-items`, `finance-export`, `maintenance-summary`, `maintenance-items`, `telegram-maintenance-settings`, `telegram-marketing-settings`, `telegram-staff-settings`, `telegram-admin-settings`, etc.) have `verify_jwt = false` in `config.toml` (Kong's HS256 gate rejects ES256 tokens) and call `verifyAdminJwt(req)` from `_shared/auth.ts` as the sole security boundary. Scheduled functions (`sd-refund-cron`, `telegram-marketing-cron`, `telegram-staff-cron`, `telegram-admin-cron`, `expire-parking-broadcasts`, `send-parking-reminders`, `calendar-sync-cron`) also use `verify_jwt = false`; `claim-parking-booking` / `decline-parking-booking` / `get-parking-broadcast-status` use `verify_jwt = false` too and instead enforce access via `verifyParkingTeamAccess(req, parkingId, ...)` scoped to the specific parking (see `[[parking-workflow]]`); hosted recurrence is configured with **`pg_cron` + `net.http_post`** (often with secrets in Vault) per [Supabase scheduling guide](https://supabase.com/docs/guides/functions/schedule-functions). Admin “Run … now” buttons call the same HTTP endpoints with a session JWT (see scheduling doc above for operational detail). Telegram crons optionally require header **`X-Telegram-Cron-Secret`** when the matching `TELEGRAM_*_CRON_SECRET` is set.

**Anti-spam & CAPTCHA** (`_shared/antiSpam.ts` → `botHeuristics.ts` + `captcha.ts` + `rateLimit.ts`; plan [[../workflow/for-testing/captcha-anti-spam-hardening|captcha-anti-spam-hardening]]): a three-layer gate — invisible bot heuristics (honeypot field `contact_time` + min-fill-time `formLoadedAt`) → Cloudflare Turnstile `siteverify` → durable fixed-window rate limit (`request_rate_limits` + `bump_rate_limit()` RPC, keyed `{scope, identity}`; identity = auth user id else client IP). Handlers call `antiSpamGate(req, body, opts)` (or `rateLimitGate(req, opts)` for rate-limit-only) and `return` the `Response` it hands back — the standard error envelope carries `captchaFailed: true` (400) or `rateLimited: true` + `retryAfterSec` (429, `Retry-After` header). Everything fails **open** on provider/DB error and **closed** on a missing/invalid token in `enforce` mode. Env: `TURNSTILE_SECRET_KEY`, `CAPTCHA_MODE` (see [[validation-and-env]] §11.2). Coverage:

- **CAPTCHA + rate limit + heuristics** (anon writes): `submit-form`, `submit-form-completion`, `submit-sd-form`, `submit-guest-review`, `submit-pay-parking`; `claim-sd-voucher` (CAPTCHA + rate limit, no heuristics — single-tap action). Client widget via `ui/src/components/security/useAntiSpamSubmit.ts`.
- **Auth OTP**: Supabase Auth native Turnstile on `signInWithOtp` (`config.toml` `[auth.captcha]`; hosted: Dashboard → Authentication → Attack Protection). Client passes `options.captchaToken` from `useCaptchaToken`.
- **Durable rate limit only** (behind the auth wall, no CAPTCHA): `submit-parking-booking-request` (+ honeypot), `submit-support-ticket` (+ honeypot), `guest-web-chat-start` / `guest-web-chat-resume` / `guest-web-chat-messages` (POST), `upload-guest-profile-asset` / `upload-guest-chat-asset` / `upload-inbox-chat-asset` / `upload-support-ticket-attachment`, `{org,property,parking}-team-invitations` (invite + resend, keyed per user + scope).
- **Authenticated wrapper default (log-only):** `serveAdmin` / `serveAuthenticated` call `logOnlyRateCheck` (120 req / 60s per user, separate `wrapper-default:` scope). Does not 429 yet. CI: `scripts/dev/check-authenticated-rate-limit.sh`. Public rate-limit **fails open**; AI quota (`assertOrgAndPropertyAiQuota`) **fails closed**.
- **SSRF:** user-supplied outbound fetches go through `_shared/safeOutboundUrl.ts` (`fetchRemoteAudioBytes`, generated-video download). Calendar iCal fetch has its own hop + private-IP + provider-allowlist loop in `calendarSyncService.ts`. Meta OAuth return origin is allowlisted (`isMetaReturnOriginAllowed`).

**CORS**: `_shared/cors.ts` echoes an allow-listed `Origin` (known hosted SPA domains, exact extra origins from `CORS_ALLOWED_ORIGINS`, and loopback `localhost` / `127.0.0.1` / `::1` on any port). Arbitrary `*.vercel.app` origins are not trusted. Add a temporary preview's exact origin to `CORS_ALLOWED_ORIGINS` when it must call hosted Edge Functions. `Access-Control-Allow-Headers` must include every header the browser actually sends, including PostHog tracing (`x-posthog-distinct-id`, `x-posthog-session-id`, `x-posthog-window-id`) and `idempotency-key`. A missing allow-listed header fails the OPTIONS preflight as `net::ERR_FAILED` / `TypeError: Failed to fetch` — that is what `[useHostGoogleAuth] post-sign-in routing failed` is when `list-organizations` never reaches the handler.

**Email HTML at runtime**: Functions that call `loadEmailTemplate()` (through `_shared/emailService.ts` or `_shared/workflowOrchestrator.ts`) must declare **`static_files`** in `supabase/config.toml` (paths relative to `supabase/`, e.g. `./functions/_shared/email-templates/*.html` and `./functions/_shared/email-templates/fragments/*.html`). The hosted Edge bundle otherwise omits those `.html` assets and `Deno.readTextFile` fails at runtime. Requires **Supabase CLI ≥ 2.7** for `static_files`; add the same globs when introducing a new function that sends email.
